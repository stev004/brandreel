Implemented within scope:

- [bin/script.mjs](/private/tmp/br-u3-script/bin/script.mjs): prompt generation, model CLI execution, fenced JSON extraction, validation, banned phrase checks, dry-run support.
- [fake-model.mjs](/private/tmp/br-u3-script/bin/tests/fixtures/fake-model.mjs): valid, invalid, and banned-phrase modes.
- [script.test.mjs](/private/tmp/br-u3-script/bin/tests/script.test.mjs): 5 new tests.
- README usage documentation.

Verification:

- New tests: 5 passed.
- Existing directory command: 7 passed.
- Smoke script: passed, produced `brand: "regulate"` and `id: "br-script-smoke"`.
- Invalid model: exited 1 with missing `coreMechanic` and total duration violations.
- No `/Users/` paths.
- No `engine/` reference in `bin/script.mjs`.
- No forbidden dash characters.

Blocker: `node --test bin/tests/` only loads the existing `bin/tests/index.js`, so it runs 7 tests rather than all 12. Including the new tests requires modifying that existing out-of-scope file. I stopped without touching it.

`git diff --stat`:

```text
 README.md | 8 ++++++++
 1 file changed, 8 insertions(+)
```

Untracked implementation files are omitted from `git diff --stat`. `engine/node_modules` was pre-existing and untouched.
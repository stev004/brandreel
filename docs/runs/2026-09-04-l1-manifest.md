Implemented Stage 5 layout manifests.

Changes:
- Added `engine/src/manifest.ts` and schema-validating `manifest-cli.ts`.
- Extended layout boxes with timing, typography, roles, text, and fit metadata.
- Added `GLYPH_EM` estimates and per-line caption elements.
- Added manifest TypeScript config and npm script.
- Integrated manifest build/emission into `bin/compose.mjs` before rendering.
- Added 4 manifest tests and `.gitignore` entry for `engine/out/`.

Verification:

- `npm run typecheck`: passed
- `npm test`: 24 passed
- `npm run manifest`: passed
- Smoke manifest: 21 elements, `closeStartMs = 25000`, goal text `estimatedLines = 3`
- `node --check bin/compose.mjs`: passed
- Bin tests: 14 passed
- Only scoped files are modified or untracked. No render was run.
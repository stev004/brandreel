Implemented within scope.

- Added `bin/manifest.mjs`; compose now delegates manifest generation.
- Added manifest stage before compose in reel dry-runs.
- Added `--no-render` lint mode with skipped render rules and `closeDwellMs`.
- Recalibrated CTA, copy, and per-beat duration limits.
- Added script lint-and-retry loop with `--retries`, `--skip-lint`, and `--lint-cmd`.
- Added fixtures and retry, duration, CTA, no-render, and stage-order tests.
- Updated README.

Verification:

- `node --test bin/tests/`: 42 passed
- Dry-run order: manifest, compose, lint, review
- Demo manifest: exit 0
- Demo no-render lint: exit 0; fps, duration, pixel-bands skipped
- Generated demo layout removed; workspace report restored
- No `/Users/` paths or prohibited dash characters
- No engine, brands, or workspace changes remain

Diff stat: 342 insertions, 98 deletions across scoped tracked files, plus new `bin/manifest.mjs` and `bin/tests/fixtures/fake-lint.mjs`.
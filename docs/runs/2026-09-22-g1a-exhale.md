# G1a exhale engine - 2026-09-22

G1a engineering acceptance is complete on `feature/G1a`. G1b remains a draft and has not been rendered or visually accepted.

## Work and review

Two gpt-5.6-luna workers at xhigh implemented the engine and the script/acceptance tests. The parent coordinated and reviewed; workers did not commit. No dependencies, installs, global brand changes, or lint-rule changes were made.

- Added a prop-driven Exhale beat, deterministic eight-second dot/fill motion, live countdown, thought dissolves, and instrument exit.
- Added optional aligned thoughtPositions to Moment. Its presence selects the approved placed narrative choreography; existing Moment scripts retain their old path.
- Added non-text geometry to the manifest separately from text elements. It records the column, track, fill, dot motion, and ticks. Non-text overlap checking remains G4b.
- Added the 19.6-second workspace script with the header copy list and identical thought positions in both scenes.
- Corrected the roadmap file list to include Moment.tsx and manifest.ts, both required by G1a's stated deliverables.

Review corrections included explicit port-header px values, a single parent exit for the IN/OUT labels, tick visibility through the scene end, title metadata ending at 6400ms, shared FPS, distinct track/fill alignment, and consistent conservative text-width estimates. Tests assert numeric manifest geometry, nonlinear easing, countdown boundaries, thought continuity, visibility lifetime, and invalid schema inputs.

## Commands actually run

From `engine/`:

```text
$ npm run typecheck
> tsc --noEmit
exit 0

$ npm test
Test Files  6 passed (6)
Tests       37 passed (37)
exit 0
```

From the repository root:

```text
$ node --test bin/tests/
tests 116
pass 116
fail 0
cancelled 0
skipped 0
todo 0
exit 0

$ node bin/manifest.mjs workspace/regulate-3am
exit 0

$ node bin/lint.mjs workspace/regulate-3am --no-render
exit 1
[safe-zone] beat-1-in-label: box right edge 997.4000000000001 exceeds 960
[safe-zone] beat-1-out-label: box right edge 1026.4 exceeds 960
[safe-zone] beat-1-out-label: box bottom edge 1688 exceeds 1600

$ git diff --check
exit 0
```

The manifest reports totalDurationMs 19600, closeStartMs 17100, firstOnScreenTextMs 150. Its column box is (727.2, 288, 144, 1344). The dot center travels from y342 to y1578 between global times 7900 and 15900ms. All five thought boxes retain identical positions across the scene cut.

No-render lint: text-fit, hook, pacing, CTA, and overlap pass. Width, height, fps, duration, and pixel-bands are skipped. This is not render validation.

## G1b blockers and resume point

1. The approved IN/OUT positions exceed the current safe zones as shown above. They were not moved to make lint pass. Steven must decide the layout change.
2. The current Close component cannot reproduce the approved centered wordmark followed by a smaller line, delayed entrances, and matching night ground. G1b's original script-only scope cannot fix this. The current script uses close.line with the kit wordmark, so its passing 2500ms CTA dwell does not establish faithful close timing.
3. The approved 2500ms close delays its line by 500ms, giving only 2000ms CTA visibility. Steven has a pending choice between extending the close by 500ms and retaining the approved duration with the conflict unresolved. No answer was assumed.
4. The current kit fonts and ink colors differ from the frozen mock. The two beat backgrounds use the approved night ground through props; no brand kit was altered. Final font/color fidelity remains part of G1b review.
5. The final rev5d CSS right offset of 58px supersedes the historical header's 44px. Header copy uses straight apostrophes where two markup strings use curly ones; the draft follows G1b's explicit header-copy instruction.

After those decisions: implement the close in a bounded engine unit, regenerate manifest, run the required pipeline, and compare rendered frames against the frozen animatic at the roadmap times. Leave G1b unchecked until its pipeline and visual evidence exist, and leave G1c's KEEP/TWEAK/KILL to Steven.

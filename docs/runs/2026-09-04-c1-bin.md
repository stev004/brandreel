Implemented within scope.

- CTA now checks rendered `close-line`, `close-tagline`, and `close-url` text with `MIN_CTA_DWELL_MS = 2500`.
- Added overlap lint and wired it into both lint modes.
- Added `--url`, model URL dropping, and close duration validation from 2500ms to 4000ms.
- Updated prompt, fixtures, README, and tests.

Verification:

- Old CTA rule: `[]`
- New CTA rule: `["[cta] CTA on screen for 1000ms; minimum 2500ms"]`
- Tests: 54 passed, 0 failed
- Smoke lint: exit 1, includes the required 1000ms CTA violation
- `ctaDwellMs: 1000`, `closeDwellMs: 1500`
- `git diff --check`: clean
- Only scoped files changed; smoke lint report restored

The sandbox could not run `git checkout` because the external worktree index was not writable, so the report was restored manually to its tracked contents.
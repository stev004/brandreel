Implemented two-row CloseD taglines.

- Added `taglineMaxLines: 2`.
- Added clamped, wrapped tagline rendering.
- Derived `urlTop` with a named 24px gap plus entrance-drift reserve; computed value: `1118.4`.
- Manifest now uses the two-row height and max-lines.
- Added regression test for `"Not meditation. Regulation."`.
- `CLOSE_D_TIMING` unchanged.
- Safe bottom check passes: URL box ends at `1193.2`, below `1600`.

Verification:

- Failing test first: received `maxLines 1`, expected `2`.
- Typecheck: passed.
- Full tests: `30 passed`.
- Guard test: passed.
- Diff scope: only 3 permitted files, `52 insertions(+), 4 deletions(-)`.
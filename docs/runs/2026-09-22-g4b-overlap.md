# G4b non-text overlap - 2026-09-22

Two Luna xhigh workers implemented geometry emission and lint checks. Parent reviewed the renderer and independently ran the acceptance checks.

## Behavior

The manifest emits Figure axis, solid and dashed bars, goal ring, flash dot and flash ring, plus CloseD logo boxes. Exhale column, track, fill, dot and ticks remain included. Geometry uses the same renderer constants and global scene offsets. Bar motion and logo entrance are recorded; a short scene clips visibility without speeding up the motion.

Overlap checks text against shapes during concurrent visibility. Existing text/text behavior stays intact. Shape/shape layering is intentional and is not checked. Moving boxes use a conservative swept rectangle over the coincident interval, including bezier extrema. Rings are opaque rectangles for this rule. The flash ring conservatively bounds its scaled shadow during the rise; the persistent dot retains its peak-sized envelope after the pulse. Such bounds may report near misses as collisions. This is layout lint, not pixel-perfect occlusion detection.

## Independent verification

```text
cd engine && npm run typecheck && npm test
Typecheck: exit 0
Test Files 8 passed (8)
Tests 42 passed (42)

node --test bin/tests/
tests 137
pass 137
fail 0
```

The bar-crossing fixture returns:

```text
[overlap] bar-label and figure-bar intersect at 100,100 during 500-1500ms
```

Fresh temporary workspace copies of each baseline script were used for these commands, preserving committed workspace artifacts:

```text
node bin/manifest.mjs <temporary-regulate-sigh> --no-measure
node bin/lint.mjs <temporary-regulate-sigh> --no-render
node bin/manifest.mjs <temporary-smoke-3am-v3> --no-measure
node bin/lint.mjs <temporary-smoke-3am-v3> --no-render
```

All four exit 0. Both reports: ok true, violations [], safe-zone/text-fit/hook/pacing/cta/overlap pass. Render-dependent checks skipped. CTA dwell: Sigh 3100ms, smoke-3am-v3 2500ms. No rendered fidelity or human KEEP claimed.

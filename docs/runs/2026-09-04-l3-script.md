Implemented within scope.

- Added `--vo` and `--music`; model modules are discarded and logged.
- Added exported `COPY_LIMITS` and enforced all requested copy limits.
- Updated prompt example and instructions.
- Added 6 regression tests and updated fake model.
- Updated README usage.

Verification:

- `node --test bin/tests/`: 20 passed, 0 failed.
- No flags smoke: exit 0, `modules` count 0.
- `--vo af_heart`: writes `modules.vo.voice` as `af_heart`.
- `git diff --check`: clean.
- Diff touches only the 4 scoped files.
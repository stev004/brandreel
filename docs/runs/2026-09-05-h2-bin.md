Implemented within scope.

- Tightened all hook predicates and mismatch reporting.
- Added `firstOnScreenText` with kicker/eyebrow render-order handling.
- Fixed decimal matching for written fact precision.
- Replaced ambiguous retry records with `retriesUsed` and `retryLimit`.
- Updated prompt, README, fixture, and regression tests.

Verification:

- Failing test-first run: red due missing `firstOnScreenText` export.
- Passing run: `116/116` tests.
- Diff scope: `README.md`, `bin/script.mjs`, `bin/tests/fixtures/fake-model.mjs`, `bin/tests/script.test.mjs`.
- `git diff --check`: passed.
- No commit created.
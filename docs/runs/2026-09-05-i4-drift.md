Implemented.

- Separated resting `h` from entrance `driftPx` across all layout elements.
- Safe-zone lint now checks `y + h + driftPx`.
- Overlap lint uses resting boxes only.
- Preserved Moment thought spacing using occupied height.
- Added overlap and safe-zone regression fixtures/tests.

Verification:

- Engine typecheck: pass
- Engine tests: 32 passed
- Bin tests: 77 passed
- Smoke manifest/lint: `smoke-ok`
- Workspace artifacts restored from `HEAD`
- Only allowed files changed; `git diff --check` passes.
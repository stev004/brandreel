ROLE: implementer
GOAL: Remove the two Figure template box collisions the new overlap lint reports, so the manifest for workspace/smoke-3am-v2 has no [overlap] violations.
SCOPE: worktree /private/tmp/br-c3-engine (branch fm/c3-engine). MAY touch: engine/src/layout.ts (FIGURE_LAYOUT constants and the figure section of computeTextBoxes), engine/src/templates/Figure.tsx (only to apply the same width/top constants it must share with the manifest), engine/tests/layout.test.ts, engine/tests/manifest.test.ts. MAY NOT touch bin/, other templates, config.ts.
CONTEXT: node bin/lint.mjs workspace/smoke-3am-v2 --no-render reports: "[overlap] beat-2-counter and beat-2-goal intersect at 420,590 during 7200-15000ms" and "[overlap] beat-2-goal and beat-2-unit-label intersect at 420,730 during 7200-15000ms". Manifest boxes for that beat (id,x,y,w,h): counter 60,470,900,228 (the counter is a short number at 190px yet its box spans the full 900px content width, so it intersects the goal text at x 420); goal 420,590,540,144 (2 rows of 60px x 1.2); unit-label 60,730,900,34 (its top is 730, 4px above the goal box bottom 734). FIGURE_LAYOUT: counterTop 470, counterFontSize 190, counterLineHeight 1.2, goalX 420, goalTop 590, goalFontSize 60, goalLineHeight 1.2, goalWidth 540, goalMaxLines 2, unitLabelTop 730. The overlap rule is strict axis-aligned intersection of boxes whose time ranges intersect (bin/lint-rules.mjs overlap()).
Required: (1) the counter box width must be the counter's own column: add counterWidth = goalX - contentX - a named gap (e.g. 24px) to FIGURE_LAYOUT, use it in the manifest box and as the counter's CSS width in Figure.tsx (the counter is left-aligned so nothing visible changes for short numbers; document that a counter longer than the column now clips, which text-fit will flag). (2) the unit label must sit below the goal box: derive unitLabelTop from goalTop + goalMaxLines * goalFontSize * goalLineHeight + a named gap (e.g. 16px) in both the manifest and Figure.tsx, or give the goal 1 row less if Steven's geometry needs the label at 730 - choose the derived top (label moves down ~20px). Keep everything else unchanged.
ACCEPTANCE:
- A test that fails on the current constants and passes after: the figure boxes for a beat like smoke-3am-v2's beat 2 do not intersect pairwise (counter vs goal, goal vs unit-label).
- cd engine && npm run typecheck exits 0; npm test exits 0 with >= 29 tests; guard test passes.
- npm run manifest, then (from the repo root) node bin/manifest.mjs workspace/smoke-3am-v2 && node bin/lint.mjs workspace/smoke-3am-v2 --no-render prints no [overlap] lines (the [cta] dwell line remains and the exit is 1; that is expected). Then restore workspace/smoke-3am-v2/layout.json and lint-report.json to their tracked contents (git checkout may be blocked in the sandbox; if so, restore by copying from git show HEAD:<path>).
- git diff --stat touches only SCOPE files.
VERIFY: cd /private/tmp/br-c3-engine/engine && npm run typecheck && npm test && npm run manifest && cd .. && node bin/manifest.mjs workspace/smoke-3am-v2 && (node bin/lint.mjs workspace/smoke-3am-v2 --no-render | grep -c overlap; true) && git diff --stat
TIMEBOX: 25 minutes.
FORBIDDEN: dependencies; git commit; bin/ edits; changing SAFE_* constants; brand literals; em/en dashes.
REPORT: failing/passing runs, new constant values, lint output, git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

ROLE: independent trail auditor (gpt-5.6-terra). Trail claims are things to verify, not evidence.
GOAL: Audit the fifth foreman run (rows from the run-start mentioning "feature/hook-validation" onward in /private/tmp/fm-brandreel-state/.claude/decisions.tsv) against git and the repo; produce an Attention list.
SCOPE: read-only in /private/tmp/fm-brandreel-state (branch feature/hook-validation). May run git log/show/diff, node --test bin/tests/, node bin/script.mjs <tmp workspace> --dry-run with briefs you write under /tmp, and read docs/runs/*, .claude/*, STATE.md, bin/script.mjs. No edits, commits, renders, installs, real model calls.
CONTEXT: Digest docs/runs/2026-09-05-digest-hook-validation.md. Check: the archetype predicates (bin/script.mjs HOOK_ARCHETYPE_RULES) for false accepts and false rejects with plausible hooks (e.g. does "No." pass contrarian; does "Why you can't sleep" pass both curiosity-gap and direct-callout; is a hook that satisfies every archetype meaningful); whether validation reads the same hook text the manifest treats as first on-screen text; the figure decimals rule when several facts share a value; script-attempts.json semantics (attempts vs retries) versus the printed line; evidence files exist and match; stale statements in STATE/FRONTIER/RUN/GATES.
ACCEPTANCE: Attention list with severity, claim, finding, reference. "No flags" is valid if true.
TIMEBOX: 15 minutes.
REPORT: the Attention list, then one final line: AUDITOR: gpt-5.6-terra.

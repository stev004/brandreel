ROLE: implementer
GOAL: bin/script.mjs stops the model inventing modules and enforces copy limits so generated scripts fit the templates.
SCOPE: worktree /private/tmp/br-l3-script (branch fm/l3-script). MAY touch: bin/script.mjs, bin/tests/script.test.mjs, bin/tests/fixtures/fake-model.mjs, README.md (script usage). Nothing else.
CONTEXT: In the last run, claude -p returned modules {vo:{voice:"regulate-narrator"}, music:{file:"audio/music.wav"}} that nobody asked for (see workspace/smoke-3am/script.json), and copy that overflowed the Figure template ("Where your nervous system stopped" as goalText at 60px in a 540px box) and a caption that truncated at 2 lines. Read bin/script.mjs and its tests first.
Required:
1. New flags --vo <voiceId> and --music <file>. modules.vo is written only when --vo is given (value from the flag), modules.music only when --music is given. Any modules the model returns are discarded (log one line "dropped model-provided modules"). The prompt tells the model not to emit modules.
2. Copy limits, stated in the prompt and enforced in validation (violation strings name the beat index and field): moment.line <= 44 chars, moment.eyebrow <= 24, each moment.thoughts entry <= 60 (max 3 thoughts); question.lines each <= 36 (max 3), question.kicker <= 24, question.dek <= 90; figure.label <= 40, figure.goalText <= 26, figure.unitLabel <= 12, figure tick labels <= 12, stamps text <= 32 (max 4); verdict.lines each <= 36; close.line <= 44, close.tagline <= 40; caption <= 2 lines and <= 44 chars per line (split on newline); hashtags 3-6 each starting with # and no spaces. Put the limits in one exported object COPY_LIMITS at the top of bin/script.mjs so a later unit can share them.
3. The prompt's example JSON must itself satisfy the limits.
ACCEPTANCE:
- node --test bin/tests/ exits 0, pass count >= 19 (14 existing + >= 5 new: --vo/--music set modules and their absence leaves modules out even when the fake model emits them; a line over a limit is rejected with the field named; caption with 3 lines rejected; hashtag without # rejected; the dry-run prompt mentions the limits).
- node bin/script.mjs /tmp/l3-smoke --brand regulate --topic "cannot sleep" --model-cmd "node bin/tests/fixtures/fake-model.mjs" exits 0 and the written script.json has no "modules" key; the same with --vo af_heart writes modules.vo.voice "af_heart".
- git diff --stat touches only SCOPE files; no /Users/ paths.
VERIFY: cd /private/tmp/br-l3-script && node --test bin/tests/ && rm -rf /tmp/l3-smoke && node bin/script.mjs /tmp/l3-smoke --brand regulate --topic "cannot sleep" --model-cmd "node bin/tests/fixtures/fake-model.mjs" && grep -c modules /tmp/l3-smoke/script.json; node bin/script.mjs /tmp/l3-smoke --brand regulate --topic "cannot sleep" --vo af_heart --model-cmd "node bin/tests/fixtures/fake-model.mjs" && grep -A2 modules /tmp/l3-smoke/script.json; git diff --stat
TIMEBOX: 30 minutes.
FORBIDDEN: dependencies; git commit; other files; calling a real model; em/en dashes; absolute user paths.
REPORT: changes and why, per-criterion outputs, git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

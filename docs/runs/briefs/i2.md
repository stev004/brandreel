ROLE: implementer
GOAL: Let the CloseD tagline wrap to two rows so a brand tagline like "Not meditation. Regulation." (27 chars) fits, with the manifest and lints agreeing.
SCOPE: worktree /private/tmp/br-i2-engine (branch fm/i2-engine). MAY touch: engine/src/layout.ts (CLOSE_D_LAYOUT + the close section of computeTextBoxes/computeTimeline), engine/src/components/Close.tsx (tagline block only), engine/tests/layout.test.ts, engine/tests/manifest.test.ts. MAY NOT touch bin/, other templates, config.ts.
CONTEXT: Steven resolved gate G6: allow two rows. CLOSE_D_LAYOUT: taglineTop 830, taglineFontSize 96, taglineLineHeight 1.2, urlTop 986, urlFontSize 34. The manifest gives close-tagline maxLines 1 (see the close section of computeTextBoxes in layout.ts) and text-fit in bin estimates 0.52em per display glyph in 900px, so 18 chars per row. Close.tsx renders the tagline in one row. The url sits at 986, which a second tagline row (830 + 2 * 115 = 1060) would collide with; the overlap lint would flag it.
Required: add taglineMaxLines: 2 to CLOSE_D_LAYOUT; the tagline box height and manifest maxLines use it; Close.tsx wraps normally and clamps to 2 rows (overflow hidden, -webkit-line-clamp like Caption.tsx); derive urlTop from taglineTop + taglineMaxLines * taglineFontSize * taglineLineHeight + a named gap (e.g. 24px) so the url never intersects the tagline box; keep CLOSE_D_TIMING unchanged. Check the total still sits above SAFE_BOTTOM (1920 - 320 = 1600).
ACCEPTANCE:
- Failing-test-first: a manifest test with close.tagline "Not meditation. Regulation." must show close-tagline maxLines 2 and estimatedLines <= 2, and no intersection between the close-tagline and close-url boxes; fails now, passes after.
- cd engine && npm run typecheck exits 0; npm test exits 0 with >= 30 tests; guard test passes.
- git diff --stat touches only SCOPE files.
VERIFY: cd /private/tmp/br-i2-engine/engine && npm run typecheck && npm test && cd .. && git diff --stat
TIMEBOX: 20 minutes.
FORBIDDEN: dependencies; git commit; bin/ edits; SAFE_* changes; brand literals; springs; em/en dashes.
REPORT: failing run, passing run, new constants, git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

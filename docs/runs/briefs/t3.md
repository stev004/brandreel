ROLE: independent trail auditor (gpt-5.6-terra; different family from the director Claude and the executor gpt-5.6-luna). Trail claims are things to verify, not evidence.
GOAL: Audit the third foreman run (rows from the run-start mentioning "feature/cta-lint" onward in /private/tmp/fm-brandreel-state/.claude/decisions.tsv) against git and the repo; produce an Attention list.
SCOPE: read-only in /private/tmp/fm-brandreel-state (branch feature/cta-lint). May run git log/show/diff, node --test bin/tests/, cd engine && npm test, node bin/manifest.mjs workspace/smoke-3am-v3, node bin/lint.mjs workspace/smoke-3am-v3 --no-render (then restore tracked files via git show HEAD:<path> if git checkout is blocked), read docs/runs/*, .claude/*, STATE.md, ROADMAP.md, bin/lint-rules.mjs, engine/src/components/Close.tsx. No edits, commits, renders, installs, network.
CONTEXT: Digest: docs/runs/2026-09-04-digest-cta-lint.md. Previous audit (docs/runs/2026-09-04-trail-audit-terra-layout-lints.md) flagged the CTA lint as trivial; this run claims to have fixed it. Check: does the new cta rule in bin/lint-rules.mjs assert elements the Close component really renders (read Close.tsx and layout.ts computeTextBoxes close section together); could it pass on a close with only a logo; is the 2500ms dwell measured from the element's actual on-screen start; the overlap rule's blind spots (elements missing from the manifest, e.g. the figure axis, ring, wordmark, logo); whether the director's one-line close max 3800 change is sound (compute the static-tail window for a CloseD close with url at +900ms and for a plain close); stale statements in STATE.md/ROADMAP.md/.claude/FRONTIER.md/GATES.md/RUN.md; evidence files exist and match; test counts rerun.
ACCEPTANCE: Attention list items with severity (high/medium/low), claim, finding, reference. "No flags" is valid if true.
TIMEBOX: 20 minutes.
REPORT: the Attention list, then one final line: AUDITOR: gpt-5.6-terra.

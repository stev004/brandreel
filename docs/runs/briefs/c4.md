ROLE: implementer
GOAL: Close three audit findings in the bin stages: the close duration limit must depend on the close variant, the CTA lint must fail closed on malformed timing, and the close tagline must come from a flag (a curated brand fact), never from the model.
SCOPE: worktree /private/tmp/br-c4-bin (branch fm/c4-bin). MAY touch: bin/script.mjs, bin/lint-rules.mjs, bin/tests/script.test.mjs, bin/tests/lint.test.mjs, bin/tests/fixtures/fake-model.mjs, README.md. Nothing else.
CONTEXT: Auditor findings (verify each first):
F1 bin/script.mjs DURATION_LIMITS.close = {min 2500, max 3800}. The engine close has two variants (engine/src/components/Close.tsx, layout.ts computeTimeline): plain close (no tagline, no url, no logo) shows line + wordmark at +0ms and nothing changes afterwards, so any plain close longer than 3000ms fails the pacing lint (static > 3000ms); CloseD (tagline and/or url) has its last visual change at +900ms (url) or +500ms (tagline only), so its static tail is durationMs - 900 or - 500. Required: validate close.durationMs against the variant: plain close max 3000; tagline-only max 3500; with url max 3800; min 2500 always (CTA dwell). Name the offsets as constants mirroring CLOSE_D_TIMING (taglineMs 500, urlMs 900) with a comment. The prompt states the variant-specific maxima.
F2 bin/lint-rules.mjs cta(): when a qualifying close element has a non-numeric fromMs, or manifest.totalDurationMs is not a number, ctaDwellMs becomes null and no violation is emitted (fails open). Required: emit "[cta] cannot measure CTA dwell: <reason>" in those cases.
F3 The model wrote close.tagline "Not meditation." (a truncated brand tagline) because the prompt let it author the tagline under an 18-char limit. Required: like close.url, any model-provided close.tagline is dropped (log one line) and a --tagline <text> flag sets it; the prompt says not to emit close.tagline; the 18-char limit still applies to the flag value (validation error if exceeded). The fake model fixture may emit a tagline to prove it is dropped.
ACCEPTANCE:
- Failing-test-first for F1 and F2 (show both runs): a plain close of 3800ms must be rejected with a message naming the variant and its maximum; a cta fixture with close-tagline fromMs "x" must produce a [cta] violation.
- node --test bin/tests/ exits 0 with pass >= 60 (54 existing + >= 6 new: plain 3800 rejected / plain 3000 accepted / tagline-only 3600 rejected / url 3800 accepted; cta malformed fromMs and missing totalDurationMs fail; model tagline dropped and --tagline sets it; --tagline over 18 chars rejected).
- node bin/lint.mjs workspace/smoke-3am-v3 --no-render still exits 0 (restore the report with git show HEAD:workspace/smoke-3am-v3/lint-report.json > that path afterwards).
- git diff --stat touches only SCOPE files; no /Users/ paths.
VERIFY: cd /private/tmp/br-c4-bin && node --test bin/tests/ && (node bin/lint.mjs workspace/smoke-3am-v3 --no-render >/dev/null; echo exit=$?) && git show HEAD:workspace/smoke-3am-v3/lint-report.json > workspace/smoke-3am-v3/lint-report.json && git status --short && git diff --stat
TIMEBOX: 25 minutes.
FORBIDDEN: dependencies; git commit; engine/ edits; real model calls; em/en dashes; absolute user paths.
REPORT: per finding: verification, failing run, passing run; acceptance outputs; git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

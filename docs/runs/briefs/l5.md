ROLE: implementer
GOAL: Make the script stage self-correcting against the layout lints: split manifest emission into bin/manifest.mjs, add a render-free lint mode, recalibrate two rules, tighten copy and duration limits to the real template geometry, and give bin/script.mjs a lint-and-retry loop.
SCOPE: worktree /private/tmp/br-l5-bin (branch fm/l5-bin). MAY touch: bin/manifest.mjs (new), bin/compose.mjs, bin/lint.mjs, bin/lint-rules.mjs, bin/script.mjs, bin/reel.mjs (add the manifest stage), bin/tests/*.test.mjs, bin/tests/index.js, bin/tests/fixtures/*, README.md. MAY NOT touch engine/ (a parallel unit is editing it), brands/, workspace/.
CONTEXT: Read bin/compose.mjs (it currently runs "npm run manifest" in engine/ then node engine/out/manifest/manifest-cli.js <props> <workspace>/layout.json before rendering), bin/lint.mjs + bin/lint-rules.mjs (rules safe-zone, text-fit, hook, pacing, cta, duration, pixel-bands; report shape with rules map), bin/script.mjs (COPY_LIMITS, validation, --vo/--music, model-cmd on stdin), bin/reel.mjs (planStages), bin/tests/*. Lint baseline on workspace/smoke-3am after the last merge is in docs/runs/2026-09-04-smoke-3am-lint-before-fixes.json: read it.
Template geometry (from engine/src/layout.ts, average glyph 0.52em display / 0.55em body / 0.6em mono): question line rows are 128px display in 860px = about 12 characters per row and each entry of question.lines is ONE row; dek 44px body in 900px = 37 chars, one row; figure goalText 60px display in 540px, 2 rows after the parallel engine fix = 16 chars per row; verdict lines 76px display in 860px = 21 chars per row, one row each; close tagline 96px display in 900px = 18 chars one row; moment line 112px display in 900px wraps up to 4 rows of 15 chars; thoughts 30px body wrap 3 rows of 54; caption 36px body 2 rows of 45; figure label 30px mono 50 chars; stamps 28px mono 53 chars.
Required:
1. bin/manifest.mjs <workspace>: reads script.json + brands/<brand>/brand.json, writes a temp props file, runs "npm run manifest --silent" in engine/ (cwd engine, paths from import.meta.url), then node engine/out/manifest/manifest-cli.js <props> <workspace>/layout.json; exit codes propagate. bin/compose.mjs calls "node bin/manifest.mjs <workspace>" instead of doing it inline. bin/reel.mjs adds a "manifest" stage before compose (always on) so dry-run shows manifest, compose, ...
2. bin/lint.mjs --no-render: skip ffprobe, duration and pixel-bands (mark "skipped"), require layout.json, run the manifest rules, write lint-report.json, exit 1 on violations.
3. Rule recalibration in lint-rules.mjs: cta passes when script.close.line is non-empty and closeStartMs < totalDurationMs (the close always carries the CTA and always ends the timeline; the old "close must start before 80%" reading was wrong); additionally report closeDwellMs = totalDurationMs - closeStartMs in the report for humans. Update the cta fixtures/tests accordingly.
4. bin/script.mjs: (a) COPY_LIMITS retuned to the geometry above: question.line 12, question.dek 36, figure.goalText 30, verdict.line 20, close.tagline 18, moment.line 44 (unchanged), moment.thought 60 (unchanged), caption 2 x 44 (unchanged); (b) new DURATION_LIMITS per beat kind in the prompt and validation: moment 2000-4000ms, question 2500-4500ms, verdict 2000-4000ms, figure 6000-9000ms; (c) after structural validation, run node bin/manifest.mjs <workspace-temp> and node bin/lint.mjs <workspace-temp> --no-render on a temp copy of the workspace (so a rejected script never overwrites an existing script.json), and on failure re-prompt the model with the violation list appended under a "Fix these violations and return the full JSON again:" line, up to --retries N (default 2); on final failure exit 1 printing the violations; on success write script.json and layout.json into the real workspace and print both paths. --dry-run and the pure validation tests keep working without the engine (the lint step is skipped when --skip-lint is passed; tests for the retry loop use a fake model that answers bad-then-good via a counter file whose path is given in an env var FAKE_MODEL_STATE, plus a stub lint by passing --lint-cmd "<command>" that reads the workspace path as argv and exits nonzero on the first call - keep it simple, the point is to prove the retry wiring without Chromium or the engine build).
ACCEPTANCE:
- node --test bin/tests/ exits 0 with pass >= 42 (35 existing + >= 7 new: manifest stage in planStages/dry-run, --no-render report shape, cta recalibration both directions, duration limits per kind rejected/accepted, question line of 13 chars rejected, retry loop succeeds on second attempt and fails after retries exhausted).
- node bin/reel.mjs workspace/demo --dry-run prints manifest, compose, lint, review in that order.
- node bin/manifest.mjs workspace/demo exits 0 and writes workspace/demo/layout.json (the engine build runs in the sandbox; no Chromium needed), then node bin/lint.mjs workspace/demo --no-render exits 0 or 1 and writes a report whose rules map marks fps/duration/pixel-bands as skipped. Remove workspace/demo/layout.json afterwards (generated; do not commit workspace changes).
- grep -rn "/Users/" bin/ returns nothing; git diff --stat touches only SCOPE files.
VERIFY: cd /private/tmp/br-l5-bin && node --test bin/tests/ && node bin/reel.mjs workspace/demo --dry-run && node bin/manifest.mjs workspace/demo && (node bin/lint.mjs workspace/demo --no-render; echo exit=$?) && cat workspace/demo/lint-report.json && git checkout workspace/demo/lint-report.json && rm -f workspace/demo/layout.json && git status --short && git diff --stat
TIMEBOX: 50 minutes.
FORBIDDEN: dependencies; git commit; touching engine/; calling a real model; committing workspace outputs; em/en dashes; absolute user paths.
REPORT: changes and why, per-criterion outputs, git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

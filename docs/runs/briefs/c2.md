ROLE: implementer
GOAL: Guarantee the Moment template's thoughts never intersect the caption block, and cover it with a test on the layout manifest.
SCOPE: worktree /private/tmp/br-c2-engine (branch fm/c2-engine). MAY touch: engine/src/layout.ts (MOMENT_LAYOUT thought constants and any derived helper), engine/tests/layout.test.ts, engine/tests/manifest.test.ts. MAY NOT touch templates, components, config.ts, bin/.
CONTEXT: engine/src/layout.ts MOMENT_LAYOUT: thoughtsTop 1150, thoughtFontSize 30, thoughtLineHeight 1.3, thoughtMaxLines 3, thoughtStep 140, thoughtEntranceDrift 24. CAPTION_LAYOUT: fontSize 36, lineHeight 1.25, maxLines 2, bottomOffset 42, positioned bottom: SAFE_BOTTOM(320) + 42, so the caption box top is 1920 - 362 - 90 = 1468 (the manifest shows caption-line-0 at y 1468). Three thoughts occupy 1150 .. 1150 + 2*140 + 141 = 1571, which intersects the caption (a new [overlap] lint in bin will flag it). A rendered frame also shows a single thought sitting directly above the caption with no breathing room.
Required: derive the thought geometry so that with thoughtMaxLines lines and up to 3 thoughts the last thought's box bottom (including thoughtEntranceDrift) sits at least 40px above the caption box top, computed from the CAPTION_LAYOUT and config constants rather than a new literal (e.g. thoughtsTop = captionTop - gap - 3 * thoughtStep, with thoughtStep reduced if needed to keep thoughtsTop >= the moment line box bottom + 40px). Keep the moment line constants unchanged. Add a test that fails on the current constants and passes after: build a manifest for a moment beat with 3 thoughts and a caption and assert no thought box intersects any caption box; also assert thoughtsTop is below the moment line box bottom.
ACCEPTANCE:
- Failing run then passing run of the new test shown.
- cd engine && npm run typecheck exits 0; npm test exits 0 with >= 28 tests; guard test passes.
- git diff --stat touches only SCOPE files.
VERIFY: cd /private/tmp/br-c2-engine/engine && npm run typecheck && npm test && cd .. && git diff --stat
TIMEBOX: 25 minutes.
FORBIDDEN: dependencies; git commit; template or component edits; changing SAFE_* or caption constants; brand literals; em/en dashes.
REPORT: failing run, passing run, the new constant values, git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

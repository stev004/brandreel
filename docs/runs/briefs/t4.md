ROLE: independent trail auditor (gpt-5.6-terra; different family from the director Claude and the executor gpt-5.6-luna). Trail claims are things to verify, not evidence.
GOAL: Audit the fourth foreman run (rows from the run-start mentioning "feature/interview" onward in /private/tmp/fm-brandreel-state/.claude/decisions.tsv) against git, the remote CI, and the repo; produce an Attention list.
SCOPE: read-only in /private/tmp/fm-brandreel-state (branch feature/interview). May run git log/show/diff, node --test bin/tests/, cd engine && npm test, node bin/manifest.mjs workspace/regulate-sigh && node bin/lint.mjs workspace/regulate-sigh --no-render (restore tracked files via git show HEAD:<path>), gh run list --branch feature/interview, and read docs/runs/*, .claude/*, STATE.md, ROADMAP.md, bin/*.mjs, engine/src/*. No edits, commits, renders, installs.
CONTEXT: Digest docs/runs/2026-09-05-digest-interview.md. Check especially: does script.mjs really enforce the brief (try to reason about a script that violates each brief rule; are facts-only numbers enforced for all figure fields; can required phrases be satisfied by the caption alone; does hookLine matching handle punctuation); is the claim "first model attempt, no retries" supported by any artifact; the driftPx change - did any safe-zone coverage get weaker (an element that previously failed safe-zone because of drift and now passes); the interview's defaults (--defaults fills unanswered questions: could a CI or unattended run produce a brief with an empty coreMechanic or audience); the tagline limit 36 vs the two-row geometry (96px display, 900px: is 36 the right number); CI run history on the branch; stale statements in STATE.md/ROADMAP.md/.claude/*.
ACCEPTANCE: Attention list items with severity (high/medium/low), claim, finding, reference. "No flags" is valid if true.
TIMEBOX: 20 minutes.
REPORT: the Attention list, then one final line: AUDITOR: gpt-5.6-terra.

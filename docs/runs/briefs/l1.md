ROLE: implementer
GOAL: Stage 5 emits a layout manifest: bin/compose.mjs writes workspace/<id>/layout.json from the engine's existing pure layout functions, extended with timing, text and fit estimates per element.
SCOPE: worktree /private/tmp/br-l1-manifest (branch fm/l1-manifest). MAY touch: engine/src/manifest.ts (new), engine/src/manifest-cli.ts (new), engine/src/layout.ts (extend computeTextBoxes only additively; existing exports and tests must keep passing), engine/tsconfig.manifest.json (new), engine/package.json scripts section ONLY (add "manifest" script; no dependency changes), engine/tests/manifest.test.ts (new), bin/compose.mjs, .gitignore (add engine/out/). MAY NOT touch templates/, components/, brands/, other bin files.
CONTEXT: engine/src/layout.ts already has computeTimeline(script, brand) -> {beats, firstOnScreenTextMs, visualChangeMs, totalDurationMs} and computeTextBoxes(script, brand) -> TextBox[] {id,x,y,w,h}. Nothing emits them. SPEC.md stage 5 says the engine emits a layout manifest that stage 7 asserts safe zones from; stage 7 lints (SPEC "Lints" section): hook (first on-screen text and first visual change <= 3000ms), safe zones (no text/logo box inside top 150px, bottom 320px, right 120px; constants SAFE_TOP/SAFE_BOTTOM/SAFE_RIGHT/SAFE_LEFT in engine/src/config.ts), pacing (no static interval > 3000ms, cadence 1.5-3s), CTA present in the final 20%. Node here is v26 (native type stripping exists but the engine's imports have no extensions, so build with tsc instead).
Design:
1. engine/src/manifest.ts exports buildManifest(script, brand) -> LayoutManifest = { width, height, fps, totalDurationMs, firstOnScreenTextMs, visualChangeMs, closeStartMs, safe: {top,bottom,left,right}, elements: Element[] } where Element = TextBox + { beatIndex: number | null (null for close/caption), role: "display" | "body" | "mono", text: string, fontSize: number, lineHeight: number, maxLines: number, letterSpacingEm: number, fromMs: number, toMs: number, estimatedLines: number }. fromMs/toMs come from the beat span (and phase-in offsets where layout.ts already knows them, e.g. thoughtPhaseInStartMs); caption element covers 0..sum of beats when script.caption is non-empty, with maxLines CAPTION_LAYOUT.maxLines and one element per caption line. estimatedLines = ceil(estimatedWidthPx / w) using a per-role average glyph width table exported from layout.ts as GLYPH_EM = { display: 0.52, body: 0.55, mono: 0.6 } (em per character incl. space) plus letterSpacingEm per character; a text that contains explicit newlines is measured per line and summed. Extend computeTextBoxes so that every element it emits now also carries the extra fields (keep TextBox assignable; add the fields optionally or return a richer type that structurally includes TextBox so tests/layout.test.ts still passes).
2. engine/src/manifest-cli.ts: reads argv[2] (props.json with {brand, script}) and writes argv[3] (layout.json). Validate brand and script with the Zod schemas from schema.ts and exit 1 with the Zod issues on failure.
3. engine/tsconfig.manifest.json: extends tsconfig.json, module commonjs, outDir out/manifest, include only src/manifest-cli.ts and what it imports; noEmit false. package.json script "manifest": "tsc -p tsconfig.manifest.json". Make sure layout.ts's imports (check: does it import remotion? if so, isolate what manifest-cli needs so the build does not pull remotion or react into the node process, e.g. by moving pure constants it needs, without changing template behaviour).
4. bin/compose.mjs: after writing props.json and BEFORE rendering, run "npm run manifest --silent" in engine/ (spawnSync) then node engine/out/manifest/manifest-cli.js <props> <workspace>/layout.json; fail if either exits nonzero. Keep the render unchanged.
ACCEPTANCE:
- cd engine && npm run typecheck exits 0; npm test exits 0 with >= 24 tests (20 existing + >= 4 new: manifest for workspace/demo/script.json + brands/regulate/brand.json has elements for every beat text, caption elements, closeStartMs = sum of beat durations, estimatedLines > maxLines for a deliberately long text, element boxes carry fromMs/toMs within their beat).
- cd engine && npm run manifest exits 0, then node out/manifest/manifest-cli.js <a props file you write to /tmp with brand + script from workspace/smoke-3am/script.json> /tmp/layout.json exits 0 and /tmp/layout.json has elements, and the element for beat 3's goalText ("Where your nervous system stopped") reports estimatedLines >= 2.
- node --check bin/compose.mjs exits 0 and grep -n "manifest" bin/compose.mjs shows the two steps.
- git diff --stat touches only SCOPE files.
VERIFY: cd /private/tmp/br-l1-manifest/engine && npm run typecheck && npm test && npm run manifest && node out/manifest/manifest-cli.js /tmp/l1-props.json /tmp/l1-layout.json && node -e 'const m=require("/tmp/l1-layout.json");console.log(m.elements.length,m.closeStartMs,m.elements.filter(e=>e.text.includes("nervous")).map(e=>[e.id,e.w,e.estimatedLines]))' && cd .. && git diff --stat
TIMEBOX: 45 minutes. Stop and report if scope is insufficient.
FORBIDDEN: new dependencies; git commit; changing any template or component; changing existing layout constants; brand literals; em/en dashes; absolute user paths.
REPORT: changes and why, per-criterion outputs, git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

ROLE: implementer
GOAL: Tighten bin/script.mjs copy and duration limits to the figure template's real tick geometry and static-tail rule, and keep the last rejected model reply for debugging.
SCOPE: worktree /private/tmp/br-l6-script (branch fm/l6-script). MAY touch: bin/script.mjs, bin/tests/script.test.mjs, bin/tests/fixtures/fake-model.mjs, README.md. Nothing else.
CONTEXT: A real model run through the new lint-and-retry loop failed after 2 retries with: "[text-fit] beat-3-min-tick: 3 lines exceeds maxLines 1" and "[pacing] visual change gap 17600-21000ms is greater than 3000ms". Manifest geometry for figure ticks (engine/src/layout.ts, mono 24px, glyph 0.6em = 14.4px per char): min-tick box x 60 w 24 (ONE character), achieved-tick x 110 w 72 (5 characters), goal-tick x 700 w 260 (18 characters). The prompt currently says "each figure tick label <= 12" so the model wrote a multi-character minTick. Figure timing: the goal marker lands at 5600ms into the beat (FIGURE_TIMING.goalMs) and nothing else moves after the last stamp, so a figure beat longer than 5600 + 3000 = 8600ms with no stamp after 5600ms is a guaranteed pacing failure; DURATION_LIMITS.figure.max is 9000.
Required:
1. COPY_LIMITS.figure: replace the single tick limit with minTick 1, achievedTick 5, goalTick 18; validation and prompt text use them by name (say in the prompt that minTick is a single character such as 0).
2. DURATION_LIMITS.figure.max = 8500; and a validation rule: if a figure beat's durationMs > 8600 (keep the rule anyway for overrides) or, more usefully, if durationMs - (last stamp offsetMs, or 5600 when no stamp is later than 5600) > 3000, reject with "[pacing] figure beat N would be static for Xms after its last event; shorten durationMs or add a stamp after 5600ms". Put the 5600 and 3000 as named constants FIGURE_LAST_EVENT_MS and MAX_STATIC_MS at the top of bin/script.mjs with a comment that they mirror engine FIGURE_TIMING.goalMs and the pacing lint.
3. On final failure, write the last model reply's parsed script to <workspace>/script-rejected.json together with the violations (as {"violations": [...], "script": {...}}) and print that path; success removes any stale script-rejected.json.
ACCEPTANCE:
- node --test bin/tests/ exits 0 with pass >= 46 (42 existing + >= 4 new: minTick "0h" rejected naming beat and field; goalTick of 18 chars accepted and 19 rejected; figure durationMs 9000 with no late stamp rejected with the [pacing] message and 8000 with a stamp at 6000 accepted; script-rejected.json written on final failure and absent after success).
- The prompt (dry-run output) mentions minTick single character and the figure static rule.
- git diff --stat touches only SCOPE files; no /Users/ paths.
VERIFY: cd /private/tmp/br-l6-script && node --test bin/tests/ && node bin/script.mjs /tmp/l6-dry --brand regulate --topic "x" --dry-run && grep -n "minTick\|static" /tmp/l6-dry/script-prompt.md | head && git diff --stat
TIMEBOX: 20 minutes.
FORBIDDEN: dependencies; git commit; other files; real model calls; em/en dashes; absolute user paths.
REPORT: per-criterion outputs, git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

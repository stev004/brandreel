ROLE: implementer
GOAL: Separate entrance drift from static layout in the manifest so the overlap lint compares resting boxes while the safe-zone lint still covers the drifted extent; fix the false overlap between stacked question lines.
SCOPE: worktree /private/tmp/br-i4-drift (branch fm/i4-drift). MAY touch: engine/src/layout.ts (computeTextBoxes and box helpers), engine/src/manifest.ts, engine/tests/layout.test.ts, engine/tests/manifest.test.ts, bin/lint-rules.mjs, bin/tests/lint.test.mjs, bin/tests/fixtures/*.json. MAY NOT touch templates, components, config.ts, bin/script.mjs, bin/lint.mjs.
CONTEXT: The script stage's lint-and-retry rejected a real script with "[overlap] beat-1-question-line-0 and beat-1-question-line-1 intersect at 60,693.12 during 3900-6500ms". QUESTION_LAYOUT (engine/src/layout.ts): linesTop 560, lineFontSize 128, lineLineHeight 1.04, lineStep 128 * 1.04 = 133.12, lineEntranceDrift 34. The manifest box for each question line is h = 128 * 1.04 + 34 = 167.12, taller than the step, so consecutive rows "intersect" by the drift even though the drift is a transient entrance translate (the row starts 34px lower and settles). The same pattern exists for moment line, thoughts, kicker, dek, verdict lines, figure stamps and close line (every *EntranceDrift constant is added into the box height in computeTextBoxes). The overlap rule (bin/lint-rules.mjs overlap()) and safe-zone rule (safeZones()) both read x,y,w,h.
Required:
1. In computeTextBoxes, box h is the resting height (lines * fontSize * lineHeight) and each element carries a new numeric field driftPx (the entrance drift, 0 when none). The manifest passes driftPx through. Update engine tests that assumed drift was inside h (search for EntranceDrift in tests).
2. bin/lint-rules.mjs: safeZones() checks y + h + driftPx against the bottom safe edge (the drift extends the element downward during entrance); overlap() uses the resting boxes only (h without drift). Update fixtures: add driftPx to fixture elements (0 where absent is acceptable - treat a missing driftPx as 0).
3. Keep every previously passing test passing, and keep the thought geometry from the last unit consistent: the thought step was derived from box height including drift; with drift removed from h the pairwise-clearance tests still pass (verify; do not loosen them).
ACCEPTANCE:
- Failing-test-first: a bin lint fixture with two stacked question lines (y 560 and 693.12, h 133.12, driftPx 34, overlapping time ranges) yields no [overlap] violation after the change and did before (show the manifest-test or bin-test failing on the current code and passing after).
- A safe-zone test: an element whose resting bottom is at 1590 with driftPx 34 must FAIL safe-zone (bottom edge 1600) and one with driftPx 0 must pass.
- cd engine && npm run typecheck exits 0; npm test exits 0 with >= 31 tests. node --test bin/tests/ exits 0 with pass >= 77 (75 existing + >= 2 new).
- From the repo root: cd engine && npm run manifest && cd .. && node bin/manifest.mjs workspace/regulate-sigh is NOT runnable (no script.json there); instead run node bin/manifest.mjs workspace/smoke-3am-v3 && node bin/lint.mjs workspace/smoke-3am-v3 --no-render exits 0, then restore both files from git show HEAD:<path>.
- git diff --stat touches only SCOPE files.
VERIFY: cd /private/tmp/br-i4-drift/engine && npm run typecheck && npm test && cd .. && node --test bin/tests/ && cd engine && npm run manifest && cd .. && node bin/manifest.mjs workspace/smoke-3am-v3 && node bin/lint.mjs workspace/smoke-3am-v3 --no-render >/dev/null && echo smoke-ok; git show HEAD:workspace/smoke-3am-v3/layout.json > workspace/smoke-3am-v3/layout.json; git show HEAD:workspace/smoke-3am-v3/lint-report.json > workspace/smoke-3am-v3/lint-report.json; git status --short; git diff --stat
TIMEBOX: 35 minutes.
FORBIDDEN: dependencies; git commit; template/component edits; changing layout constants (fix the model of drift, not the numbers); em/en dashes; absolute user paths.
REPORT: failing/passing runs, acceptance outputs, git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

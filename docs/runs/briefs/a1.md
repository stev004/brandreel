ROLE: independent auditor. Implementation claims are things to verify, not evidence.
GOAL: Audit two just-merged units in the brandreel repo for correctness against their acceptance criteria and the repo hard rules, and give a verdict.
SCOPE: read-only in /private/tmp/fm-brandreel-state (branch feature/phase2-machine). You may run: cd engine && npm run typecheck && npm test; node --test bin/tests/; node bin/reel.mjs workspace/demo --dry-run; git log/diff. Do not edit files, do not commit, do not run compose/polish (no Chromium).
CONTEXT: Unit U1 (commit 43c1ee4): karaoke word-timed captions - engine/src/captions.ts captionWindow(), engine/src/components/Caption.tsx, Stack.tsx/Root.tsx pass optional words, bin/compose.mjs forwards workspace/words.json into props. Unit U2 (commit 0f1f1fb): bin/review.mjs review sheet and bin/reel.mjs orchestrator (stage order vo, align, compose, polish, lint, review gated by script.modules). Rules: SPEC.md (stage 5 captions: 1-2 short lines, active-word highlight, inside safe zones; stage 8 review sheet), .claude/DIRECTOR.md hard rules (no brand literals in engine/src, 60fps single constant, no springs, no cross-stage imports, no absolute user paths, plain hyphens).
Check specifically: (1) captionWindow edge cases - words with zero-length or overlapping times, timeMs exactly at a boundary, windows whose boundaries could move mid-word (reflow), behaviour during gaps; (2) Caption.tsx computes time from useCurrentFrame relative to the Sequence it is inside (the caption Sequence starts at frame 0 so this is fine - confirm), and clamps to safe zones; (3) reel.mjs resolves stage paths relative to the repo root not cwd, propagates nonzero exits, passes --music, and --from/--to/--skip semantics are sane; (4) review.mjs handles lint-report.json in its real shape (see workspace/demo/lint-report.json) and a missing one; (5) any hex, font-name or brand-word literal in engine/src outside tests; (6) any /Users/ path or em/en dash in the diff.
ACCEPTANCE: a findings list with file:line, severity (blocker / should-fix / nit), and a reproduction or reasoning for each.
VERIFY: as in SCOPE.
TIMEBOX: 25 minutes.
FORBIDDEN: edits, commits, network, npm install.
REPORT: findings list, then exactly one final line: VERDICT: PASS | PASS-WITH-NITS | FAIL - <one sentence>.

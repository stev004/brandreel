ROLE: implementer
GOAL: Make bin/script.mjs validate the brief's hook archetype structurally, check the remaining figure fields, and record the generation attempt count as an artifact.
SCOPE: worktree /private/tmp/br-h1-bin (branch fm/h1-bin). MAY touch: bin/script.mjs, bin/tests/script.test.mjs, bin/tests/fixtures/fake-model.mjs, bin/tests/fixtures/*.json (new fixtures ok), README.md. Nothing else.
CONTEXT: An audit found: (1) brief.hookArchetype reaches the prompt but validation never reads it, so a direct-callout brief accepts a numbered-promise opener; (2) figure value.decimals and stamps[].offsetMs are not checked against anything; (3) the digest claimed "first attempt" but the stage records no attempt count. Read bin/script.mjs (brief loading, validateScript, the retry loop around line 500-760) and bin/tests/script.test.mjs first. The hook text is the first beat's first on-screen text: moment.line, question.lines[0], verdict.lines[0], or figure.label. SPEC.md stage 1 names the four archetypes: curiosity gap, contrarian claim, direct callout, numbered promise.
Required:
1. HOOK_ARCHETYPE_RULES, one exported object, each archetype a checkable predicate on the hook text (lowercased, trimmed) with a one-line description that also goes into the prompt:
   - numbered-promise: contains a digit, or a number word from one..twelve, or an ordinal (first, second, third), or "ways"/"steps"/"reasons".
   - curiosity-gap: ends with "?" or contains one of: why, what, how, secret, nobody, no one, never, until, before, after, actually, really.
   - contrarian-claim: does NOT end with "?" and contains one of: not, isn't, aren't, don't, doesn't, won't, can't, never, wrong, stop, forget, myth, no.
   - direct-callout: ends with "?" or contains you / your / you're / yours, or starts with an imperative from a short list (try, stop, start, notice, breathe, put, take, imagine, remember, listen, look).
   Validation: when brief.hookArchetype is set, the hook text must satisfy its predicate; violation names the archetype and quotes the hook text and the rule description. If brief.hookLine is also set, the exact-match rule stays and the archetype rule applies to that line too (so an incompatible brief is reported at interview time as well: bin/interview.mjs is out of scope, so check it in script.mjs when loading the brief and exit 1 with a clear message).
2. Figure fields: value.decimals must be an integer 0-2 and equal to the number of decimals of the matching fact number (the fact whose value equals value.to); stamps[].offsetMs must be integers >= 0 and < beat.durationMs, strictly increasing; violation messages name the beat and field. Document in the prompt block.
3. Attempt record: the retry loop writes <workspace>/script-attempts.json after finishing: { attempts: N, retries: M, outcome: "accepted" | "rejected", violationsPerAttempt: [[...], ...] }, and prints "script: accepted on attempt N of M+1" or "script: rejected after N attempts". Tests use --skip-lint and the fake model's bad-then-good mode.
ACCEPTANCE:
- Failing-test-first for the hook rule: a direct-callout brief with first beat line "Three ways to reset." is accepted now and rejected after; show both runs. Then one accepting and one rejecting case per archetype (8 tests) and one incompatible hookLine case.
- Figure: decimals 17 rejected; decimals mismatch with the fact rejected; stamps out of order or beyond durationMs rejected; a valid figure accepted.
- Attempt record: after a bad-then-good run, script-attempts.json has attempts 2 and outcome accepted; after a rejected run, outcome rejected.
- node --test bin/tests/ exits 0 with pass >= 100 (85 existing + >= 15 new).
- git diff --stat touches only SCOPE files; no /Users/ paths; no em/en dashes.
VERIFY: cd /private/tmp/br-h1-bin && node --test bin/tests/ && git diff --stat
TIMEBOX: 40 minutes.
FORBIDDEN: dependencies; git commit; other files; real model calls; em/en dashes; absolute user paths.
REPORT: per requirement: failing run, passing run; test count; git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

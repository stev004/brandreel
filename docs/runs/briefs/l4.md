ROLE: implementer
GOAL: Fix the engine geometry and timeline facts that the new layout lints exposed on workspace/smoke-3am: Figure goal marker crosses the right safe zone, Figure goalText has no wrapping box, and thought dissolves are not counted as visual changes.
SCOPE: worktree /private/tmp/br-l4-engine (branch fm/l4-engine). MAY touch: engine/src/layout.ts, engine/src/manifest.ts, engine/src/templates/Figure.tsx, engine/src/templates/Moment.tsx (only if needed for thoughts), engine/tests/layout.test.ts, engine/tests/manifest.test.ts. MAY NOT touch anything else (no bin/, no other templates, no config.ts constants).
CONTEXT: Lint baseline on smoke-3am (rendered by the director): "[pixel-bands] right band at 12500-17500ms has 0.8-1.1% divergent pixels" during the figure beat, and "[text-fit] beat-3-goal: 3 lines exceeds maxLines 1", and "[pacing] visual change gap 480-4000ms" during a 4000ms moment beat with one thought (thought phases in at 480ms then nothing changes until the beat ends, although the thought dissolves with blur+drift before the end - that dissolve is a visual change and computeTimeline in engine/src/layout.ts does not record it).
Facts: config.ts SAFE_RIGHT=120, WIDTH=1080, so the last allowed x is 960. FIGURE_LAYOUT: axisX 60, axisWidth 900 (axis ends at 960), goalRingLeft 934 + goalRingSize 46 = 980 (crosses by 20px), flashRingSpread 48 around the flash dot; goalX 420, goalTop 590, goalFontSize 60. Figure.tsx renders goalText at left goalX / top goalTop with no width (see the goalText block near line 145) so it runs to the frame edge; the manifest (engine/src/manifest.ts, via computeTextBoxes in layout.ts) gives it maxLines 1. Thought timing helpers: thoughtPhaseInStartMs, thoughtStaggerMs, thoughtDissolveStartMs in layout.ts; computeTimeline records phase-ins (around line 270-290) but not dissolves.
Required:
1. FIGURE_LAYOUT: shrink axisWidth so the goal ring (and any flash ring at the achieved end, and the marker halo if one is drawn) stays within x <= 960 with its full extent; derive goalRingLeft from the axis end rather than a separate literal (e.g. axisEnd - goalRingSize / 2). Keep the axis visually anchored at axisX 60.
2. Figure goalText: give it an explicit width of WIDTH - SAFE_RIGHT - goalX (add goalWidth and goalMaxLines: 2 to FIGURE_LAYOUT), wrap normally, clamp to goalMaxLines with overflow hidden; manifest element for the goal uses w = goalWidth and maxLines = goalMaxLines, and its box height = goalMaxLines lines.
3. computeTimeline: add each thought's dissolve start (thoughtDissolveStartMs) as a visual change when it falls inside the beat. Also add the moment line's own entrance if it is not already there (check).
ACCEPTANCE:
- Failing-test-first for each of the three: a test in engine/tests/layout.test.ts or manifest.test.ts that fails on the current code and passes after (ring box right edge <= 960 for a figure with axis.goal = axis.max; goal element w = 540, maxLines = 2; a 4000ms moment with one thought yields a visual change strictly between the phase-in and the beat end). Show both runs.
- cd engine && npm run typecheck exits 0; npm test exits 0 with >= 27 tests; tests/guard.test.ts still passes (no hex or font literals in templates/components).
- npm run manifest exits 0 and node out/manifest/manifest-cli.js with props built from brands/regulate/brand.json + workspace/smoke-3am/script.json shows beat-3-goal with w 540 and maxLines 2 (estimatedLines will still be 3; that is copy, fixed elsewhere).
- git diff --stat touches only SCOPE files.
VERIFY: cd /private/tmp/br-l4-engine/engine && npm run typecheck && npm test && npm run manifest && node -e 'const fs=require("fs");fs.writeFileSync("/tmp/l4-props.json",JSON.stringify({brand:JSON.parse(fs.readFileSync("../brands/regulate/brand.json","utf8")),script:JSON.parse(fs.readFileSync("../workspace/smoke-3am/script.json","utf8"))}))' && node out/manifest/manifest-cli.js /tmp/l4-props.json /tmp/l4-layout.json && node -e 'const m=require("/tmp/l4-layout.json");console.log(m.elements.filter(e=>e.id.includes("goal")).map(e=>[e.id,e.x,e.w,e.maxLines,e.estimatedLines]), m.visualChangeMs.slice(0,8))' && cd .. && git diff --stat
TIMEBOX: 40 minutes.
FORBIDDEN: dependencies; git commit; touching bin/ or config.ts; changing SAFE_* values; brand literals; springs; em/en dashes.
REPORT: per requirement: failing run, passing run; acceptance outputs; git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

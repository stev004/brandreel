ROLE: implementer
GOAL: Add bin/review.mjs (stage 8 review sheet) and bin/reel.mjs (thin orchestrator chaining the existing stage CLIs per script.json modules), with node:test coverage.
SCOPE: worktree /private/tmp/br-u2-bin (branch fm/u2-bin). MAY touch: bin/review.mjs (new), bin/reel.mjs (new), bin/tests/ (new dir, *.test.mjs), README.md (add usage lines). MAY NOT touch engine/, brands/, audio/, other bin files, workspace/ contents.
CONTEXT: Read SPEC.md (Architecture, stage 8, workspace file layout) and .claude/DEVTEAM.md. Existing stage CLIs and their contracts (read them, do not modify): bin/vo.py <workspace> (needs modules.vo; reads vo.json, writes vo.wav + vo-timing.json; run via audio/.venv/bin/python), bin/align.py <workspace> (writes words.json), bin/compose.mjs <workspace> (writes render.mp4), bin/polish.mjs <workspace> [--music <file>] (writes final/), bin/lint.mjs <workspace> (writes lint-report.json, nonzero on violation). Script schema: engine/src/schema.ts (Script has id, brand, coreMechanic, modules.vo/music optional, beats, close, caption, hashtags). A sample workspace with script.json and lint-report.json is workspace/demo.
bin/review.mjs <workspace>: reads script.json (required) and lint-report.json (optional, say "lint not run" if absent), writes workspace/review.md containing: title (script.id, brand), coreMechanic, KEEP / TWEAK / KILL checkboxes as "- [ ] KEEP", "- [ ] TWEAK", "- [ ] KILL", the caption, hashtags on one line, first comment and alt text taken from script.firstComment / script.altText when present else "TODO", a lint summary (pass/fail per check from lint-report.json in whatever shape that file has), and a file list of workspace outputs that exist (render.mp4, final/*.mp4, vo.wav, words.json) with paths relative to the repo root. Exit 0 on success, 1 with a message on missing script.json.
bin/reel.mjs <workspace> [--from <stage>] [--to <stage>] [--skip a,b] [--music <file>] [--dry-run]: stage order vo, align, compose, polish, lint, review. vo and align run only when script.modules.vo is set; polish runs when modules.vo or modules.music is set (pass --music through). Each stage is a child process of the existing CLI (node or audio/.venv/bin/python, paths relative to repo root resolved from import.meta.url); stop at first nonzero exit with the stage name. --dry-run prints one line per stage that would run, in order, as "stage: <command>", and runs nothing. Put the plan computation in an exported pure function planStages(script, options) so it is testable without spawning.
ACCEPTANCE:
- node --test bin/tests/ exits 0 with at least 6 tests: planStages with/without modules.vo, with modules.music, --from/--to/--skip, and review.md content for a temp workspace (checkboxes, hashtags line, TODO fallbacks, "lint not run").
- node bin/reel.mjs workspace/demo --dry-run prints exactly "compose: ...", "lint: ...", "review: ..." lines in that order and nothing is executed.
- node bin/review.mjs workspace/demo exits 0 and workspace/demo/review.md contains "- [ ] KEEP", "- [ ] TWEAK", "- [ ] KILL".
- grep -rn "/Users/" bin/ returns nothing.
- git diff --stat touches only files in SCOPE.
VERIFY: cd /private/tmp/br-u2-bin && node --test bin/tests/ && node bin/reel.mjs workspace/demo --dry-run && node bin/review.mjs workspace/demo && cat workspace/demo/review.md && git diff --stat && git status --short
TIMEBOX: 40 minutes. If the change needs files outside SCOPE, stop and report why.
FORBIDDEN: npm install or dependencies (node built-ins only); git commit; modifying existing bin files; running vo/align/compose/polish for real (no Chromium or venv in the sandbox); em/en dashes; absolute user paths.
REPORT: print changes and why, each acceptance criterion with actual command output, git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

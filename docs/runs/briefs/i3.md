ROLE: implementer
GOAL: Make consecutive Moment thoughts never intersect each other: the thought step must be at least one thought box tall, with the box height derived from the same constants, and prove it with a manifest test.
SCOPE: worktree /private/tmp/br-i3-engine (branch fm/i3-engine). MAY touch: engine/src/layout.ts (MOMENT_LAYOUT thought constants and helpers only), engine/src/templates/Moment.tsx (only if it hardcodes a thought height or clamp that must follow the constant), engine/tests/manifest.test.ts, engine/tests/layout.test.ts. MAY NOT touch bin/, other templates, config.ts, CLOSE_* constants (another unit is editing the close section of layout.ts; keep your diff confined to the MOMENT thought lines to avoid a merge conflict).
CONTEXT: CI lint on workspace/demo reports "[overlap] beat-0-thought-0 and beat-0-thought-1 intersect at 60,1183 during 1430-4500ms" for every beat with several thoughts. Cause: MOMENT_LAYOUT (engine/src/layout.ts) now has thoughtsTop 1079 and thoughtStep 104 (derived last run so three thoughts clear the caption box at y 1468 with 40px to spare), but a thought box in the manifest is thoughtMaxLines(3) * thoughtFontSize(30) * thoughtLineHeight(1.3) + thoughtEntranceDrift(24) = 141px tall, taller than the step. Constraints that must all hold: step >= box height; thoughtsTop + 2 * step + box height <= caption top - 40 (caption top = HEIGHT - SAFE_BOTTOM - CAPTION_LAYOUT.bottomOffset - CAPTION_LAYOUT.fontSize * CAPTION_LAYOUT.lineHeight * CAPTION_LAYOUT.maxLines = 1468); thoughtsTop >= moment line box bottom + 40 (momentLineTop 550 + 4 * 112 * 1.02 + drift 32 = 1039). With thoughtMaxLines 3 these cannot all hold (3 * 141 = 423 > 1428 - 1079 = 349), so reduce thoughtMaxLines to 2 (box = 2 * 39 + 24 = 102) and derive thoughtStep = box height + a named gap (e.g. 8px) and thoughtsTop from the caption top downward as last run did; verify the moment-line clearance still holds. Two rows of 30px body text hold about 108 characters; the script stage's thought limit is 60, so nothing is lost.
ACCEPTANCE:
- Failing-test-first: a manifest test with a moment beat carrying three thoughts asserts pairwise no intersection between thought boxes AND no intersection with caption boxes AND thoughtsTop >= moment line bottom + 40; show it failing now and passing after.
- cd engine && npm run typecheck exits 0; npm test exits 0 with >= 30 tests; guard test passes.
- git diff --stat touches only SCOPE files.
VERIFY: cd /private/tmp/br-i3-engine/engine && npm run typecheck && npm test && cd .. && git diff --stat
TIMEBOX: 20 minutes.
FORBIDDEN: dependencies; git commit; bin/ edits; SAFE_* or caption constant changes; touching CLOSE_* lines; brand literals; em/en dashes.
REPORT: failing run, passing run, new constants, git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

ROLE: implementer
GOAL: Make the CTA lint assert what the close template actually renders with a minimum on-screen dwell, add an overlap lint, and stop bin/script.mjs from letting the model invent close.url or a too-short close.
SCOPE: worktree /private/tmp/br-c1-bin (branch fm/c1-bin). MAY touch: bin/lint-rules.mjs, bin/lint.mjs, bin/script.mjs, bin/tests/*.test.mjs, bin/tests/fixtures/*, README.md. MAY NOT touch engine/, brands/, workspace/.
CONTEXT: An audit found the cta rule in bin/lint-rules.mjs (around line 104) only checks script.close.line is non-empty and closeStartMs < totalDurationMs, which passes on text the close never renders: engine/src/components/Close.tsx switches to a "CloseD" layout (logo, tagline, url) whenever close.tagline, close.url or brand.wordmark.logoSvg exist, and then close.line is NOT rendered. The manifest (workspace/<id>/layout.json, built by node bin/manifest.mjs <workspace>) already reflects this: it emits elements with ids close-line and close-wordmark for the plain close, or close-logo / close-tagline / close-url for CloseD, each with fromMs/toMs/x/y/w/h/text. Example from workspace/smoke-3am-v2/layout.json: close-tagline fromMs 25000 toMs 26000, close-url fromMs 25400 toMs 26000, totalDurationMs 26000; the model invented close.url "https://example.com". SPEC.md lints: "CTA present in final 20% of timeline". Steven's gate G5 default: the CTA is any rendered close text element (close-line, close-tagline or close-url; logo and wordmark do not count) and it must be on screen for at least 2500ms.
Required:
1. lint-rules.mjs cta(manifest, script): collect manifest elements whose id is close-line, close-tagline or close-url and whose text is non-empty; fail "[cta] no rendered CTA text element in the close" when none; ctaDwellMs = totalDurationMs - min(fromMs of those); fail "[cta] CTA on screen for Xms; minimum 2500ms" when below MIN_CTA_DWELL_MS = 2500 (named constant, exported); also fail if the earliest CTA fromMs < 0.8 * totalDurationMs is NOT satisfied... careful: SPEC wants the CTA present in the final 20%, which any close element satisfies since the close ends the timeline, so do not add a start-position rule; just report ctaDwellMs in the lint report (replace closeDwellMs with ctaDwellMs, keep closeDwellMs too).
2. New rule overlap(manifest): for every pair of text elements whose time ranges intersect (fromMs < other.toMs and other.fromMs < toMs) and whose boxes intersect (axis-aligned, strict), emit "[overlap] <idA> and <idB> intersect at <x,y> during <ms-ms>". Elements from the same beat with the same prefix that are designed to stack (e.g. beat-N-thought-0/1/2, caption-line-0/1, question lines) must be laid out not to intersect, so no exemptions; if a fixture shows a designed overlap, report it rather than exempt it. Wire it into lint.mjs (both modes) with a rules entry "overlap".
3. script.mjs: drop any model-provided close.url (log "dropped model-provided close.url"); new flag --url <text> sets close.url; the prompt tells the model not to emit close.url. close.durationMs: the prompt asks for 2500-4000ms and validation requires close.durationMs present and within DURATION_LIMITS.close = {min: 2500, max: 4000} (add to DURATION_LIMITS). Update the prompt example accordingly.
ACCEPTANCE:
- Failing-test-first for the cta change: a fixture manifest with only close-tagline fromMs 25000 of 26000 plus a script whose close.line is non-empty must FAIL under the new rule and passed under the old one; show both runs.
- node --test bin/tests/ exits 0 with pass >= 54 (46 existing + >= 8 new: cta no-element, cta dwell short, cta dwell ok with tagline only, overlap pass, overlap fail with two intersecting boxes in overlapping time, overlap pass when times do not intersect, script drops close.url and --url sets it, close.durationMs 2000 rejected / 3000 accepted).
- node bin/lint.mjs workspace/smoke-3am-v2 --no-render exits 1 and the violations include a [cta] dwell line (1000ms) - this proves the gate on the real artifact; restore workspace/smoke-3am-v2/lint-report.json with git checkout afterwards.
- git diff --stat touches only SCOPE files; no /Users/ paths.
VERIFY: cd /private/tmp/br-c1-bin && node --test bin/tests/ && (node bin/lint.mjs workspace/smoke-3am-v2 --no-render; echo exit=$?) && git checkout workspace/smoke-3am-v2/lint-report.json && git status --short && git diff --stat
TIMEBOX: 40 minutes.
FORBIDDEN: dependencies; git commit; engine/ edits; real model calls; em/en dashes; absolute user paths.
REPORT: per requirement with outputs, failing/passing runs, git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

ROLE: implementer
GOAL: Make bin/script.mjs's structural limits imply the hook, pacing and CTA lints, so a script that passes structural validation cannot fail those three manifest rules.
SCOPE: worktree /private/tmp/br-i5-limits (branch fm/i5-limits). MAY touch: bin/script.mjs, bin/tests/script.test.mjs, bin/tests/fixtures/fake-model.mjs, README.md. Nothing else.
CONTEXT: A brief-driven real-model script was rejected after retries with: "[hook] first visual change after 0ms must be at or before 3000ms; got 4000", "[pacing] visual change gap 0-4000ms is greater than 3000ms", "[cta] CTA on screen for 2300ms; minimum 2500ms". The rejected script (workspace/regulate-sigh/script-rejected.json) opened with a moment beat of 4000ms with no thoughts (its only visual event is the line at 0ms), and closed with a tagline-only close of 2800ms (tagline appears at +500ms, so dwell is 2300). Engine timing facts (engine/src/layout.ts): moment: eyebrow/line at start, thoughts phase in at min(entranceMs, 12% of duration) staggered 1.75 * entranceMs, dissolve later; question: kicker +200, lines from +600 staggered 300, dek +2400; figure: events up to goalMs 5600 plus stamps; verdict: lines from +200 staggered 1200; close plain: everything at +0; CloseD: tagline +500, url +900. Lints: hook needs a visual change at or before 3000ms; pacing forbids any static interval over 3000ms; CTA dwell >= 2500ms from the first rendered close text element. Current DURATION_LIMITS in bin/script.mjs: moment 2000-4000, question 2500-4500, verdict 2000-4000, figure 6000-8500, close min 2500 with variant maxima plain 3000 / tagline 3500 / url 3800.
Required, all as named constants with a comment naming the lint they mirror:
1. Moment beats with no thoughts: max 3000ms (a moment with at least one thought keeps 4000). Validation message names the beat and says "add a thought or shorten".
2. Verdict beats: the last line appears at 200 + 1200 * (lines - 1); require durationMs - lastLineMs <= 3000 (so 1 line: max 3200, 2 lines: max 4000 as today, 3 lines: max 4000 as today).
3. Question beats: static tail after the dek (2400) or after the last line when no dek (600 + 300 * (lines - 1)); require the tail <= 3000 (so no dek and 1 line: max 3600; with dek: max 4500 as today).
4. Close min per variant: plain 2500, tagline-only 3000, with url 3400 (dwell = duration - 500 or - 900 must be >= 2500). Keep the variant maxima.
5. The prompt states these rules in one compact block so the model can satisfy them first time; the prompt example must satisfy them.
ACCEPTANCE:
- Failing-test-first: a moment beat of 4000ms with no thoughts is rejected (fails now, passes after); a tagline-only close of 2800ms is rejected; a plain close of 2500 is accepted; a with-url close of 3300 is rejected and 3400 accepted; a 1-line verdict of 3600 rejected and 3200 accepted; a question with no dek and 1 line at 4000 rejected, 3600 accepted.
- node --test bin/tests/ exits 0 with pass >= 84 (77 existing + >= 7 new).
- git diff --stat touches only SCOPE files; no /Users/ paths.
VERIFY: cd /private/tmp/br-i5-limits && node --test bin/tests/ && git diff --stat
TIMEBOX: 25 minutes.
FORBIDDEN: dependencies; git commit; other files; real model calls; em/en dashes; absolute user paths.
REPORT: per rule: failing run, passing run; test count; git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

ROLE: implementer
GOAL: Tighten the hook archetype predicates so trivial or cross-category hooks fail, validate the text that is actually first on screen, match figure decimals by the fact's written form, and make the attempts record unambiguous.
SCOPE: worktree /private/tmp/br-h2-bin (branch fm/h2-bin). MAY touch: bin/script.mjs, bin/tests/script.test.mjs, bin/tests/fixtures/fake-model.mjs, README.md. Nothing else.
CONTEXT: Audit findings on bin/script.mjs HOOK_ARCHETYPE_RULES (around line 43): "No." passes contrarian; "Why you can't sleep" passes curiosity-gap, contrarian and direct-callout; "Three reasons why you can't sleep" passes all four. For question beats validation reads lines[0] but the template renders the kicker first (+200ms) before the line (+600ms), see engine/src/layout.ts computeTimeline. Figure decimals matching picks the first fact with the same numeric value, so facts "3.5" and "3.50" are indistinguishable. script-attempts.json writes retries = the configured limit, and the console says "attempt 2 of 3".
Required:
1. Predicates become shape checks with minimums, each still one exported predicate with a description used in the prompt:
   - numbered-promise: a number (digit, one..twelve, or an ordinal) AND a plural payoff noun within the hook (ways, steps, reasons, signs, things, mistakes, habits, minutes, seconds, breaths, days, nights) AND at least 3 words.
   - curiosity-gap: at least 4 words AND (ends with "?" OR contains a withholding phrase: "the reason", "what nobody", "what no one", "the one thing", "until", "the truth", "actually", "really") AND does NOT contain a number+plural-noun pair (that is a numbered promise).
   - contrarian-claim: a statement (no "?"), at least 3 words, contains a negation (not, isn't, aren't, don't, doesn't, won't, can't, never, no) AND a content word after the negation (the negation is not the last word), AND does not start with a second-person address ("you", "your").
   - direct-callout: at least 2 words AND (contains you / your / you're / yours OR starts with an imperative verb from the list) AND is not a numbered promise.
   Add an "ambiguous" report: when the hook satisfies more than one archetype, that is allowed, but the violation message for a mismatch lists which archetypes it does satisfy so the author can fix the brief or the copy.
2. Hook text: the validated hook is the FIRST on-screen text in render order: for a question beat that is the kicker when present, else lines[0]; for a moment beat the eyebrow when present, else the line; for figure the label; for verdict lines[0]. Export firstOnScreenText(beat) and use it for both the archetype and hookLine checks; the prompt tells the model that a kicker or eyebrow, when present, IS the hook and must satisfy the archetype (or omit it on the first beat).
3. Figure decimals: compare the fact's written form (the substring of the fact string that parsed to the number) with value.to formatted at value.decimals; accept when any fact's written form has the same number of decimals as value.decimals and the same numeric value.
4. script-attempts.json: { attempts, retriesUsed, retryLimit, outcome, violationsPerAttempt }; console "script: accepted on attempt N (M retries used of L)" / "script: rejected after N attempts (L retries)". Keep the old keys out.
ACCEPTANCE:
- Failing-test-first for the three quoted hooks: "No." rejected for contrarian; "Why you can't sleep" accepted for curiosity-gap and rejected for contrarian; "Three reasons why you can't sleep" accepted only for numbered-promise; show failing then passing runs.
- A question first beat with kicker "NOTICE" and lines[0] "Can't meditate?" under direct-callout is rejected naming the kicker as the hook; with no kicker it is accepted.
- Facts "3.50" with value.to 3.5 decimals 2 accepted; decimals 1 accepted only if a fact reads "3.5"; decimals 3 rejected.
- Attempts record: bad-then-good yields attempts 2, retriesUsed 1, retryLimit 2.
- node --test bin/tests/ exits 0 with pass >= 114 (102 existing + >= 12 new); existing archetype tests updated where the tightened rules change them (say which).
- git diff --stat touches only SCOPE files; no /Users/ paths; no em/en dashes.
VERIFY: cd /private/tmp/br-h2-bin && node --test bin/tests/ && git diff --stat
TIMEBOX: 40 minutes.
FORBIDDEN: dependencies; git commit; other files; real model calls; em/en dashes; absolute user paths.
REPORT: per requirement: failing run, passing run; test count; git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

ROLE: implementer
GOAL: Make the engine's Caption component render karaoke word-timed captions from words.json when it exists in the workspace, keeping the current static caption as the fallback.
SCOPE: worktree /private/tmp/br-u1-captions (branch fm/u1-captions). MAY touch: engine/src/components/Caption.tsx, engine/src/captions.ts (new), engine/src/Stack.tsx, engine/src/Root.tsx, engine/src/layout.ts, engine/src/schema.ts, engine/tests/captions.test.ts (new), bin/compose.mjs. MAY NOT touch anything else (no brands/, no templates/, no package.json, no node_modules).
CONTEXT: Read SPEC.md stage 5 ("Caption component: 1-2 short lines, high contrast, active-word highlight, positioned inside safe zones") and .claude/DEVTEAM.md. Today engine/src/components/Caption.tsx ignores its words prop and prints script.caption statically; engine/src/Stack.tsx line 59 renders <Caption brand={brand} text={script.caption} /> for the whole beats span; bin/compose.mjs writes {brand, script} props and never reads words.json. engine/src/schema.ts already exports Words ({words:[{text,startMs,endMs}]}), which bin/align.py writes to workspace/<id>/words.json. Timing helpers: engine/src/config.ts (FPS=60, msToFrames), easing in engine/src/ease.ts, layout constants in engine/src/layout.ts (CAPTION_LAYOUT). Colors and fonts come ONLY from brand props (brand.palette.*, resolveFonts(brand)).
Design: add a pure function in engine/src/captions.ts: captionWindow(words, timeMs, opts:{maxWordsPerLine, maxLines}) -> {lines: string[][] , activeIndex: number|null} that groups words into fixed windows of maxLines*maxWordsPerLine words (window boundaries do not move while a word is active, so text does not reflow), returns the window containing timeMs (or the next upcoming window during a gap before it starts; null lines after the last word ends + 500ms), and marks the active word (startMs <= t < endMs). Caption uses useCurrentFrame to compute timeMs, renders the window with the active word in brand.palette.accent and others in brand.palette.fg; when words is undefined, current static behavior is unchanged. Stack accepts optional words and passes it through; Root.tsx props schema accepts optional words; compose.mjs reads workspace/words.json if present, validates nothing beyond JSON parse, and adds it to props as "words".
ACCEPTANCE:
- engine/tests/captions.test.ts exists with at least 5 cases covering: window grouping, active index inside a word, gap between words (no active), before first word (upcoming window), after last word + 500ms (empty).
- cd engine && npm run typecheck exits 0.
- cd engine && npm test exits 0 (existing 12 tests + new ones), including tests/guard.test.ts (no hex literals, no springs).
- grep -n "words" bin/compose.mjs shows words.json being read conditionally and added to props.
- git -C /private/tmp/br-u1-captions diff --stat touches only the files in SCOPE.
VERIFY: cd /private/tmp/br-u1-captions/engine && npm run typecheck && npm test ; cd .. && git diff --stat
TIMEBOX: 40 minutes. If the change needs files outside SCOPE, stop and report why.
FORBIDDEN: npm install or any new dependency; git commit; reformatting or renaming untouched code; per-composition fps; springs; any hex or font literal in engine/src; em/en dashes.
REPORT: print changes and why, each acceptance criterion with the actual command output, and git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

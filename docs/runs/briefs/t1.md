ROLE: independent trail auditor (different model family from the director, Claude, and the executor, gpt-5.6-luna). Claims in the trail are things to verify, not evidence.
GOAL: Audit the foreman run recorded in /private/tmp/fm-brandreel-state/.claude/decisions.tsv against what actually happened in git and the repo, and produce an Attention list.
SCOPE: read-only in /private/tmp/fm-brandreel-state (branch feature/phase2-machine). You may run git log/show/diff, node --test bin/tests/, cd engine && npm test, and read docs/runs/*.md, .claude/*.md, STATE.md, ROADMAP.md, workspace/*/lint-report.json. Do not edit, commit, render, or install.
CONTEXT: The run's predicate: karaoke captions, bin/review.mjs, bin/reel.mjs, bin/script.mjs landed on feature/phase2-machine with gates green and the demo rendered + linted by the director. Trail rows reference commits 43c1ee4 (U1), 0f1f1fb (U2), U3 script stage, fe75208 (U4), and an audit report docs/runs/2026-09-04-audit1-u1u2.md whose FAIL verdict the director partly overruled.
Check: each trail row's evidence resolves (commit exists, report file exists, counts match what you can rerun); verification claims that have no artifact (renders are gitignored - say so where that weakens the claim); risky-in-hindsight decisions (e.g. overruling the literal-hexes finding, committing workspace/smoke-3am/script.json generated by a model, gitignoring review.md); stale mentions in STATE.md, ROADMAP.md, .claude/FRONTIER.md, .claude/GATES.md, .claude/RUN.md (SHAs, statuses, counts that contradict git); anything the executor reports claim that the merged code does not do.
ACCEPTANCE: an Attention list, each item: severity (high/medium/low), what was claimed, what you found, file or commit reference. "No flags" is valid if true.
TIMEBOX: 20 minutes.
FORBIDDEN: edits, commits, network, installs.
REPORT: the Attention list, then one final line: AUDITOR: gpt-5.6-terra.

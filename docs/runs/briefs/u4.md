ROLE: implementer
GOAL: Fix four audit findings from an independent review of the just-merged units, with failing-test-first for each.
SCOPE: worktree /private/tmp/br-u4-fixes (branch fm/u4-fixes). MAY touch: bin/reel.mjs, bin/review.mjs, bin/tests/index.js, bin/tests/*.test.mjs, engine/src/captions.ts, engine/tests/captions.test.ts. MAY NOT touch anything else.
CONTEXT: Auditor findings (verify each yourself before fixing):
F1 bin/reel.mjs around line 124: explicit --music does not activate the polish stage because planStages is computed from script.modules.music only, before the CLI music option is considered. Repro: node bin/reel.mjs workspace/demo --dry-run --music audio/custom.wav prints only compose, lint, review. Required: --music (flag or =form) activates polish and is passed through as "--music <file>" in the polish command, in both planStages and the dry-run output.
F2 bin/review.mjs around line 110: the review sheet omits the CTA that SPEC.md stage 8 requires. Required: a "## CTA" section rendering script.close.line, plus tagline and url when present, each on its own line; the KEEP/TWEAK/KILL section stays as is.
F3 engine/src/captions.ts around line 41: window selection can jump to the next window while a word in the current window is still active, given zero-length or overlapping word timings, and exposes the next window early during long gaps. Required rule (deterministic, half-open): let k = the index of the last word with startMs <= timeMs (none if timeMs is before the first word's startMs, in which case show window 0); show the window containing k; after the last word's endMs + 500ms show nothing; activeIndex = the word in the shown window with startMs <= timeMs < endMs, choosing the highest index if several match. Keep the exported signature captionWindow(words, timeMs, opts) unchanged.
F4 bin/tests/index.js only imports reel-review.test.mjs so node --test bin/tests/ never runs bin/tests/script.test.mjs (5 tests). Required: node --test bin/tests/ runs all 12 tests (import script.test.mjs from index.js, or whatever makes the directory form load every *.test.mjs).
ACCEPTANCE:
- For F1, F2, F3: add a test that fails on the current code and passes after the fix; show both runs in the report.
- node --test bin/tests/ exits 0 and reports pass >= 14 (12 existing + new).
- node bin/reel.mjs workspace/demo --dry-run --music audio/custom.wav prints polish with --music audio/custom.wav between compose and lint.
- cd engine && npm run typecheck exits 0 and npm test exits 0 with >= 19 tests.
- git diff --stat touches only SCOPE files.
VERIFY: cd /private/tmp/br-u4-fixes && node --test bin/tests/ && node bin/reel.mjs workspace/demo --dry-run --music audio/custom.wav && cd engine && npm run typecheck && npm test && cd .. && git diff --stat && git status --short
TIMEBOX: 30 minutes.
FORBIDDEN: dependencies; git commit; touching files outside SCOPE; reformatting; em/en dashes; absolute user paths; brand literals.
REPORT: per finding: verification of the finding, the failing test output before, passing after; then acceptance outputs; git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

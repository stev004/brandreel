ROLE: implementer
GOAL: Add bin/script.mjs, the stage 1 LLM script stage: topic + brand.json -> a validated workspace/<id>/script.json, via a configurable model CLI, with a fake-model test path.
SCOPE: worktree /private/tmp/br-u3-script (branch fm/u3-script). MAY touch: bin/script.mjs (new), bin/tests/script.test.mjs (new), bin/tests/fixtures/ (new), README.md (usage lines). MAY NOT touch engine/, brands/, other bin files, workspace/.
CONTEXT: Read SPEC.md stage 1 and the Script schema in engine/src/schema.ts (authoritative shape: id, brand, coreMechanic (one sentence, required), modules {vo?:{voice}, music?:{file}}, beats (discriminated by kind: moment | question | figure | verdict, see each beat's fields and durationMs), close {line, showWordmark, tagline?, url?, durationMs?}, caption, hashtags). Read brands/regulate/brand.json for the brand fields available (name, palette, fonts, wordmark, motion, voice.tone, voice.notes). Existing bin CLIs for style (bin/review.mjs, bin/reel.mjs, bin/tests/*.test.mjs use node:test and node built-ins only). Stages talk through files only: do not import engine/ code; reimplement the structural checks you need in plain JS.
Usage: node bin/script.mjs <workspace> --brand <name> --topic "<text>" [--model-cmd "<shell command that reads the prompt on stdin and prints the model reply on stdout>"] [--duration-ms <target, default 25000>] [--dry-run]. Default model-cmd is "claude -p". Behaviour: build a prompt containing the brand voice (tone list, notes verbatim), the four hook archetypes from SPEC (curiosity gap, contrarian claim, direct callout, numbered promise), the required JSON shape written out as an example with every beat kind, the rules (coreMechanic is one sentence; every on-screen string lives in the beats; total beat durationMs within 15000-35000; 4-8 beats; caption <= 150 chars; 3-6 hashtags; respond with JSON only, no prose). --dry-run writes workspace/<id>/script-prompt.md and exits 0 without calling the model. Otherwise spawn model-cmd via the shell with the prompt on stdin, extract the first JSON object from stdout (tolerate code fences), then validate: required keys present, coreMechanic non-empty, brand equals --brand, id equals the workspace basename (override it), every beat.kind in the allowed set with a positive durationMs, total duration 15000-35000, hashtags array, and none of the phrases listed in brand.voice.notes after a line starting with "Banned:" (if present) appear case-insensitively in any string. On validation failure exit 1 printing each violation; on success write script.json pretty-printed and print its path.
ACCEPTANCE:
- bin/tests/fixtures/fake-model.mjs exists: reads stdin, prints a valid script JSON wrapped in a markdown code fence. A second fixture (or a flag/env var) makes it print an invalid one (missing coreMechanic, duration too long).
- node --test bin/tests/ exits 0 with at least 12 tests total (7 existing + >= 5 new: dry-run writes the prompt containing the tone words; fake model happy path writes script.json with id = workspace basename; invalid reply exits nonzero listing violations; banned phrase is rejected; fenced JSON is extracted).
- node bin/script.mjs /tmp/br-script-smoke --brand regulate --topic "cannot sleep at 3am" --model-cmd "node bin/tests/fixtures/fake-model.mjs" exits 0 and the written script.json parses with brand "regulate".
- grep -rn "/Users/" bin/ returns nothing; grep -rn "engine/" bin/script.mjs returns nothing.
- git diff --stat and git status show only files in SCOPE.
VERIFY: cd /private/tmp/br-u3-script && node --test bin/tests/ && node bin/script.mjs /tmp/br-script-smoke --brand regulate --topic "cannot sleep at 3am" --model-cmd "node bin/tests/fixtures/fake-model.mjs" && cat /tmp/br-script-smoke/script.json && git status --short
TIMEBOX: 40 minutes. Stop and report if you need scope beyond SCOPE.
FORBIDDEN: dependencies; git commit; touching other bin files or engine/; calling any real model (no network); em/en dashes; absolute user paths; inventing brand values.
REPORT: changes and why, per-criterion actual outputs, git diff --stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

ROLE: implementer
GOAL: Fix captionWindow so the shown window never leaves a still-active word, failing-test-first.
SCOPE: worktree /private/tmp/br-u5-captions (branch fm/u5-captions). MAY touch: engine/src/captions.ts, engine/tests/captions.test.ts. Nothing else.
CONTEXT: An auditor found that engine/src/captions.ts (around line 33) picks the window of the latest-starting word even when an earlier word is still active, so with overlapping or zero-length timings the caption can jump windows mid-word. The U4 test used a four-word window and masked the two-word case. Required rule, in priority order: (1) if any word is active (startMs <= timeMs < endMs), show the window containing the highest-index active word and mark it active; (2) else if timeMs is before the first word's startMs, show window 0 with no active word; (3) else if timeMs >= last word's endMs + 500ms, show nothing; (4) else show the window containing the last word with startMs <= timeMs, no active word. Window = fixed groups of maxWordsPerLine*maxLines words. Keep the exported signature.
ACCEPTANCE:
- A new test with maxWordsPerLine 2, maxLines 1 and words A[0,1000] Z[500,500] C[600,900] asserting at t=500 window is [A,Z] with A active, at t=700 window is [C] with C active (C is the highest-index active word), at t=950 window is [A,Z] with A active. Show it failing on the current code, then passing.
- cd engine && npm run typecheck exits 0; npm test exits 0 with >= 20 tests.
- git diff --stat touches only the two SCOPE files.
VERIFY: cd /private/tmp/br-u5-captions/engine && npm run typecheck && npm test; cd .. && git diff --stat
TIMEBOX: 15 minutes.
FORBIDDEN: dependencies; git commit; any file outside SCOPE; brand literals; em/en dashes.
REPORT: failing run, passing run, diff stat.
STANDING:
## Hard rules (pasted into every brief as STANDING)
- SPEC.md rulings are settled: Remotion, eight file-contract stages, brand.json Zod boundary, 60fps global (one constant, engine/src/config.ts), deterministic bezier only, no springs. Deviations need Steven.
- Stages talk through files in workspace/<id>/ only. No cross-stage imports.
- Zero brand literals (hex, font names, brand words) in engine templates; guard tests enforce. Brand facts only in brands/<name>/brand.json, curated never invented.
- No absolute user paths. Plain hyphens in docs and copy; no em/en dashes or U+2212.
- Codex sandbox: no Chromium, no network, no npm install, no new dependencies. Stop at `npm run typecheck` + `npm test` from engine/ (and `node --test` for bin). Renders are director-run.
- Never commit from the executor; the director commits. main is protected; Steven merges.
- No secrets, no .env.

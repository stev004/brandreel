Implemented all three findings.

F1

- Verification: close limits now vary by variant: plain 3000ms, tagline-only 3500ms, URL 3800ms; minimum remains 2500ms.
- Failing run: targeted plain-close test exited 1 before implementation.
- Passing run: full suite includes plain 3800ms rejection, plain 3000ms acceptance, tagline-only 3600ms rejection, and URL 3800ms acceptance.

F2

- Verification: malformed CTA `fromMs` and missing `totalDurationMs` now emit `[cta] cannot measure CTA dwell: ...`.
- Failing run: targeted malformed-timing test exited 1 before implementation.
- Passing run: both fail-closed CTA tests pass.

F3

- Verification: model taglines are dropped and logged; `--tagline` supplies the value; values over 18 characters are rejected. Prompt and README updated.
- Passing run: provenance and length tests pass.

Acceptance:

- `node --test bin/tests/`: 61 passed, 0 failed
- Smoke lint: `exit=0`
- Report restored from `HEAD`
- `git diff --check`: passed
- Only scoped files modified

Diff stat:

```text
README.md                 |  2 +-
bin/lint-rules.mjs        |  8 +++++++
bin/script.mjs            | 36 ++++++++++++++++++++++++----
bin/tests/lint.test.mjs   | 14 +++++++++++
bin/tests/script.test.mjs | 61 +++++++++++++++++++++++++++++++++++++++++++----
5 files changed, 110 insertions(+), 11 deletions(-)
```
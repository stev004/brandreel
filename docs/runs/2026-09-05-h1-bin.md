Implemented on `fm/h1-bin`.

- Added exported `HOOK_ARCHETYPE_RULES` and structural hook validation.
- Added incompatible brief hook-line rejection.
- Added figure decimals and stamp offset validation.
- Added `script-attempts.json` records and status output.
- Added bad-then-good fake model coverage.

Verification:

- Failing-test-first run: failed before implementation because `HOOK_ARCHETYPE_RULES` was absent.
- Passing run: `node --test bin/tests/`
- Result: 102 passed, 0 failed.
- Diff scope: only 3 allowed files, 265 insertions, 11 deletions.
- No commit made.
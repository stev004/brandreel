# Foreman run digest - 2026-09-04 - Phase 2 engineering

Predicate: karaoke captions from words.json, bin/review.mjs, bin/reel.mjs orchestrator, bin/script.mjs LLM stage landed on feature/phase2-machine with typecheck + tests green and the demo rendered + linted by the director. **MET.**

Iterations: 3 (plus digest). Codex runs: 5 implementation (luna@high) + 1 audit (sol@high) + 1 trail audit (terra@high). All on the state branch; no remote exists, nothing pushed.

Landed (all merged --no-ff into feature/phase2-machine):
- U1 43c1ee4 karaoke captions (engine/src/captions.ts, Caption.tsx, compose forwards words.json)
- U2 0f1f1fb bin/review.mjs + bin/reel.mjs + bin/tests
- U3 bin/script.mjs (topic + brand.json -> script.json via a model CLI, default claude -p; fake-model tests)
- U4 fe75208 audit fixes: reel --music activates polish, review CTA section, test index
- U5 caption window invariant (never leave an active word)
- Proof: workspace/smoke-3am generated by claude -p (sonnet) and pushed through compose -> lint -> review; frames in docs/runs/frames/.

Discarded: nothing. Overruled: audit finding that Root.tsx preview defaults and the fonts.ts loader registry are brand literals (rule scoped in DIRECTOR.md).

Open gates (GATES.md): G1 create a GitHub remote (default: not yet); G2 merge phase2-machine into main (default: hold, Steven merges); G3 music source (taste, untouched); G4 templates vs copy limits for layout lints (default: both, next run).

Attention list from the trail audit (gpt-5.6-terra), all accepted: caption overlap bug (fixed by U5); smoke-3am's invented modules.vo/music are unproven (script.mjs must stop the model inventing modules; next run); renders and frames were unfiled (now under docs/runs/); STATE/FRONTIER SHAs were stale (fixed; rule added); digest was missing (this file); briefs unfiled (now under docs/runs/briefs/).

Next run's obvious predicate: stage 5 emits a layout manifest and stage 7 asserts safe zones, text fit, hook <= 3s, pacing and CTA placement; smoke-3am must fail the new lints, then pass after template fixes; script.mjs may only set modules when asked. Also: pin Remotion versions (caret drift warning on every render).

# STATE - brandreel snapshot

*Updated 2026-09-22 (G4a merged; G4b overlap acceptance passed). Snapshot of current truth; history in SESSIONS.md and git.*

## Where things stand
- **Current work branch: `feature/G4b`, acceptance passed.** G4a merged and pushed at `a3fe22f`, with CI green. G4b emits Figure and CloseD geometry and checks text against opaque shapes over coincident visibility intervals. Typecheck, 42 engine tests, 137 CLI tests pass; fresh Sigh and smoke-3am-v3 manifests pass layout lint. Evidence: `docs/runs/2026-09-22-g4b-overlap.md`. Steven authorized merging verified engineering milestones and continuing until a human decision is needed. Next unblocked goal: G4c Remotion pins.
- **Branch chain (remote: https://github.com/stev004/brandreel, public, CI renders on push):** `main` <- `feature/m1-engine` <- `feature/howclose-trial` <- `feature/regulate-kit` <- `feature/phase2-machine` (merged to main fce5a38) <- `feature/layout-lints` (merged to main 2fd01a5) <- `feature/cta-lint` (merged to main fb3f599) <- `feature/interview` (merged 24e275f) <- `feature/regulate-3am` (merged ff24466, 09-13). <- `feature/hook-validation` (merged 09-22: hook archetype predicates, figure field checks, script-attempts.json). No live worktrees; fm/* branches are merged and disposable. `engine/node_modules` on main is a real `npm ci` install (09-22).
- **Gates all green on main after G4a (09-22):** typecheck, 39 engine tests, 130 bin tests, CI green (`node --test bin/tests/`). Chain: `node bin/script.mjs workspace/<id> --brand regulate --topic "..."` (claude -p; retries against the manifest lints) -> `node bin/reel.mjs workspace/<id> --skip vo,align,polish` (manifest -> compose -> lint -> review). workspace/regulate-sigh is the first brief-driven video (`node bin/interview.mjs <ws> --answers <file>` then `node bin/script.mjs <ws>` then `node bin/reel.mjs <ws>`); passes all 11 lint rules. Audio runs only when the brief sets voice/music. smoke-3am* are kept baselines. Audio venv not present in these worktrees (rebuild per audio/README.md).
- **Lint coverage on the work branch:** ffprobe facts, safe zones, real-font text fit with explicit estimate fallback, hook, pacing (static > 3s), CTA (rendered close text element per the manifest, on screen >= 2500ms), text overlap, pixel bands. `bin/manifest.mjs` measures by default using installed Chromium and renderer font metadata; `--no-measure` or `BRANDREEL_NO_MEASURE=1` selects the offline path. Browser/font failures retain estimates with warnings. G4b adds non-text overlap with conservative motion bounds.

## Waiting on Steven
-1. ~~KEEP/TWEAK/KILL on `animatics/regulate-3am.html`~~ **KEEP (09-13, rev5d) - animatic frozen; next build step = the port** (see Next build steps 3). Previously rev5d (Steven on rev5c: keep once the dot is at the top - fixed in rev5d, strip-verified; expected KEEP -> port brief next). Previously rev5c (rev5 had a pulse + an overlap; rev5c: transparent scenes, invisible switch, instrument exits before the close; strip-verified). Previously rev5 (rev4 "best we've genuinely had" + a true 900ms crossfade into the breathing; the mock's scene switch was a snap-out, now fixed in the player). Previously rev4 (rev3 "best so far" + Steven's three tweaks: app-style entrance into the breathing, two more thoughts, the breathing game = the app's Regulate Breath instrument; 18.5s). Verified with `bin/animatic-frames.mjs` (real-time headless frames). Previously: rev3 = the approved mock verbatim (rev1 transcribed the port, rev2 re-rendered the mock scaled - both rejected; SPEC ruling 11 + amendment). Three TWEAK questions in its header: emerald ground vs the mock's night black, teal vs sage dot, Playfair vs Bodoni. (branch `feature/regulate-3am`, artifact link in the hivemind STATE). Steven's call 09-05: the 3am video is the first Regulate video; the Sigh is second. Plan + port notes: `animatics/regulate-3am.plan.md`. After KEEP: port needs an `exhale` beat kind (see plan).
0. ~~Merge feature/hook-validation~~ done 09-22.
0a. **KEEP/TWEAK/KILL on workspace/regulate-sigh** as a text-only video is superseded: The Sigh gets its own animatic first (ROADMAP G3a). The workspace stays as the brief + pipeline proof.
0b. **Regulate demo re-render for a look** (optional): `workspace/demo/render.mp4` in the reg worktree now shows Playfair wordmark + pure white dot. The demo's copy ("let the night be enough", "a softer landing") is placeholder and softer than the Brand Book voice - the first real Regulate script replaces it, not a copy edit.
1. **Fusion animatic PARKED at rev6** (Steven 09-03: "still not great - save it, iterate later"). Saved into the howclose.to repo at `marketing/animatics/fusion-can-we-bottle-a-star.html` (commit 62abe3f there); working copy stays here. Iteration levers + full arc: ROADMAP.md Phase 1.
2. **Merge** the chain (command above).

## Open threads
- **2026-09-22 G1a complete on `feature/G1a`:** two Luna xhigh workers implemented the engine and acceptance fixture/tests; the parent reviewed, requested corrections, and independently ran all gates. The G1a file list now includes Moment.tsx and manifest.ts, required by its stated placement and geometry deliverables. The workspace script remains a G1b draft until fidelity checks pass.
- **G1b safe-zone decision pending:** authored IN label right edge is estimated at 997.4px, OUT at 1026.4px (limit 960px); OUT bottom including entrance drift is 1688px (limit 1600px). Positions are preserved. Moving these labels changes the approved layout and needs Steven's taste decision. Text-fit, hook, pacing, current CTA, and text overlap pass; media and pixel checks are not run.
- **G1b close dependency:** the existing Close component places a large line above a small wordmark; the approved animatic centers the wordmark above a smaller line. Its ground also comes from the current kit instead of the approved scene ground. A close implementation extension is needed outside G1b's script-only file list. Do not count a successful render or current close lint as proof of fidelity.
- **G1b timing decision pending:** approved close duration is 2500ms, with the line entering at 500ms, leaving only 2000ms of visible CTA against the 2500ms lint minimum. Steven has been asked whether to extend the close by 500ms or preserve the approved timing and leave the conflict unresolved. Do not weaken the lint or silently change timing.
- **G1b source differences to review:** final rev5d CSS moves the column right offset from the historical header's 44px to 58px. Use the final CSS for that geometry. The header copy list uses straight apostrophes where the markup has curly apostrophes; the draft follows G1b's explicit header-copy requirement. Current kit fonts and ink colors differ from the frozen mock; no global brand edits have been authorized or made.

## Next build steps (in order) - the authoritative list is ROADMAP.md (goal ladder G0-G9); this is the short form
1. v2 port after gate: new `bars` beat kind (statement zone + IN/OUT bars + shot attempts + zoom + need-bar), 1:1 from `animatics/howclose-fusion-v2.html` rev6. **Scene durations must derive from `vo-timing.json` when modules.vo is on** - measured VO is 33s vs 29s picture; VO timing is authoritative.
2. Assemble the finished v2 video: compose -> polish (real music track needed - none chosen yet) -> lint -> review sheet.
3. **First real Regulate video = 3:04 AM - animatic KEPT 09-13 (rev5d, 19.6s).** G1a engine and manifest acceptance passed 09-22 on `feature/G1a`. Next for G1b: resolve the label safe-zone and close timing decisions, implement faithful close support, then compose -> lint -> frame comparison -> review. See Open threads. Second video = The Sigh (workspace/regulate-sigh exists as a brief-driven text version; the orb-free breath-paced animatic is still to author).
4. Lint accuracy (G4), footage M3 (G5), extract-brand + photography kit (G6), music fetcher (G7), fusion (G8), distribution (G9).

## Brand sources (taste step reads these first)
- **regulate:** Brand Book = source of truth (`~/Documents/regulate-hivemind/marketing/BRAND_BOOK.html`, artifact 2c016ffe-80f4-4378-ab52-18db5187d841). Laws: dot always white · a state colour names a state · emerald is the ground, white is theirs · teal is a supplement. Display Playfair (app's Cormorant is drift). `brands/regulate/brand.json` voice.notes carries the laws + bans.
- **howclose:** brand book artifact a9221676-6c46-4d8d-9edf-2a2af0df1c1e + repo docs.

## Environment facts (hard-won)
- **Animatic verification (strip):** `node bin/animatic-strip.mjs <file> <out> <from-ms> <to-ms>` gives ~80ms frames named by the page clock; tile with ffmpeg and READ THE STRIP, not single frames - rev5's pulse and overlap were invisible in stills. One run per strip.
- **Animatic verification (frames):** the in-app Browser pane freezes CSS animations while hidden (document.visibilityState = hidden, every animation at currentTime 0) and Chrome's --virtual-time-budget stalls rAF players. Use `node bin/animatic-frames.mjs <file> <out-dir> <ms...>` - real-time headless Chrome via DevTools protocol; frames are named by actual capture time (~2s per frame).
- Audio venv MUST be Python 3.12 (`python3.12 -m venv audio/.venv`) - kokoro has no 3.14 wheels. Setup in `audio/README.md`.
- Codex sandbox: no Chromium, no network - renders and installs are reviewer-run.
- Kokoro new API yields Result objects (.audio), not tuples - handled in bin/vo.py.
